# 生产级量化交易系统开发计划

## 1. 项目重新定位

本项目的第一目标是构建一套能够连接真实交易账户、可靠发送委托、管理订单和持仓、处理异常并恢复运行的自动交易系统。

系统开发顺序调整为：

1. 打通端到端真实交易链路；
2. 建立可靠的订单、成交、资金和持仓管理；
3. 建立基础风控、状态恢复和人工控制能力；
4. 接入简单策略，验证长期自动运行；
5. 建立行情、回测和策略研究体系；
6. 扩展因子计算和策略组合；
7. 最后引入 LLM 进行因子发现和研究自动化。

系统必须做到：

> 即使没有高级策略、没有回测平台、没有 LLM，交易系统本身也能够可靠地查询账户、接收行情、发送订单、管理持仓、处理异常并记录完整交易过程。

---

# 2. 核心开发原则

## 2.1 先完成单市场、单账户、单接口

第一阶段不要同时支持 A 股、期货、多个券商和多个账户。

应选择一条最短路径：

* A 股优先：选择一个已经开通程序化接口的券商账户，接入其 QMT、PTrade 或其他交易接口；
* 期货优先：选择一个能够通过 TqSdk、CTP 或其他接口交易的期货账户。

先完成：

```text
一个账户
→ 一个交易接口
→ 一个品种
→ 一笔查询
→ 一笔下单
→ 一笔成交
→ 一次撤单
→ 一次持仓核对
```

只有这一条链路稳定后，才抽象多券商适配层。

## 2.2 第一版使用模块化单体

第一版不需要微服务、Kafka、Kubernetes 或复杂分布式架构。

建议采用：

```text
Python 交易进程
├── Broker Adapter
├── Trading Gateway
├── Order Manager
├── Position Manager
├── Account Manager
├── Risk Manager
├── Strategy Runner
├── Reconciliation
└── PostgreSQL
```

模块之间保持清晰接口，但部署时可以运行在一个进程或少量进程中。

这样更容易：

* 调试；
* 重放问题；
* 保证状态一致；
* 避免分布式事务；
* 快速打通实盘链路。

等到系统确实需要多账户、多市场和高吞吐，再拆分服务。

---

# 3. 第一阶段：打通端到端交易链路

## 3.1 第一阶段目标

第一阶段不考虑收益率，也不考虑复杂策略。

唯一目标是：

> 从自己的程序发出一个交易意图，经过交易前置和券商系统，最终获得委托、成交、资金和持仓变化，并在本地正确记录整个过程。

完整链路如下：

```text
人工或测试策略生成交易指令
        ↓
基础交易前检查
        ↓
Order Manager 创建本地订单
        ↓
Broker Adapter 转换接口参数
        ↓
交易前置 / 券商 API
        ↓
交易所或柜台返回委托状态
        ↓
接收委托、成交和错误回报
        ↓
更新订单状态
        ↓
更新本地持仓和资金
        ↓
与券商查询结果核对
```

## 3.2 必须实现的账户查询能力

系统首先要能够可靠查询：

* 账户连接状态；
* 账户基本信息；
* 总资产；
* 可用资金；
* 冻结资金；
* 当前持仓；
* 可卖数量；
* 当日委托；
* 当日成交；
* 未完成订单；
* 历史或当日资金变化。

所有查询结果都要转换为自己的统一数据结构，而不是让上层代码直接依赖券商接口对象。

例如：

```python
@dataclass
class Position:
    account_id: str
    symbol: str
    total_quantity: Decimal
    available_quantity: Decimal
    frozen_quantity: Decimal
    average_price: Decimal
    market_value: Decimal
    unrealized_pnl: Decimal
    updated_at: datetime
```

## 3.3 必须实现的交易能力

第一阶段至少支持：

* 限价买入；
* 限价卖出；
* 市价或对手价委托；
* 撤单；
* 查询单笔订单；
* 查询所有活动订单；
* 接收订单状态回报；
* 接收成交回报；
* 接收拒单和错误信息。

第一版暂时不需要实现：

* TWAP；
* VWAP；
* 冰山单；
* 智能拆单；
* 跨账户路由；
* 高频订单管理。

## 3.4 端到端验收测试

第一阶段必须真实完成以下测试：

1. 启动程序并连接交易前置；
2. 查询资金和持仓；
3. 发送一笔价格明显不会立即成交的限价单；
4. 查询到该订单；
5. 撤销该订单；
6. 确认订单最终为已撤销；
7. 发送一笔可以成交的小额订单；
8. 接收委托确认；
9. 接收成交回报；
10. 查询到新的持仓；
11. 重启系统；
12. 从券商重新恢复订单、资金和持仓状态；
13. 反向交易并恢复原始仓位；
14. 核对全部订单、成交、费用和资金变化。

完成这些测试，才算“交易链路跑通”。

---

# 4. 第二阶段：订单管理系统

仅仅成功调用下单 API 不等于拥有可靠交易系统。

## 4.1 本地订单模型

每笔订单都必须先在本地创建，再发送给券商。

核心字段：

```text
client_order_id
broker_order_id
account_id
strategy_id
symbol
side
order_type
price
quantity
filled_quantity
average_fill_price
status
reject_reason
created_at
submitted_at
acknowledged_at
updated_at
```

其中 `client_order_id` 必须由本地系统生成并全局唯一。

它用于：

* 防止重复下单；
* 关联策略信号；
* 关联券商订单；
* 恢复系统状态；
* 排查异常交易。

## 4.2 订单状态机

统一订单状态：

```text
CREATED
    ↓
RISK_CHECKED
    ↓
SUBMITTING
    ↓
SUBMITTED
    ↓
ACKNOWLEDGED
    ├── PARTIALLY_FILLED
    ├── FILLED
    ├── CANCEL_PENDING
    │       └── CANCELLED
    ├── REJECTED
    └── UNKNOWN
```

券商接口返回的各种状态，都必须映射到自己的统一状态。

## 4.3 禁止无条件重试下单

最危险的场景是：

1. 系统发送了订单；
2. 请求超时；
3. 实际上券商已经收到；
4. 系统认为失败并再次发送；
5. 最终产生重复订单。

因此：

* 查询请求可以重试；
* 下单请求不能直接无条件重试；
* 发生超时后，订单进入 `UNKNOWN`；
* 必须先查询券商委托记录；
* 确认不存在原订单后，才能重新发送。

---

# 5. 第三阶段：持仓与资金管理

## 5.1 券商状态是真实来源

初期不要完全依赖本地计算持仓。

系统应同时维护：

* 本地推导持仓；
* 券商查询持仓；
* 两者差异。

券商查询结果作为最终真实状态，本地状态用于：

* 实时响应；
* 策略计算；
* 风险控制；
* 异常检测。

## 5.2 持仓变化来源

持仓只能通过以下事件改变：

* 买入成交；
* 卖出成交；
* 期货开仓成交；
* 期货平仓成交；
* 公司行为；
* 交割；
* 手工交易；
* 外部系统交易；
* 人工校准。

禁止策略直接修改持仓数量。

## 5.3 持仓核对

系统启动时、交易过程中和收盘后都要执行核对。

```text
本地持仓
    ↕
本地成交记录
    ↕
券商成交记录
    ↕
券商当前持仓
```

发现差异时：

* 记录详细差异；
* 暂停自动开仓；
* 允许查询和人工处理；
* 根据券商状态重新同步；
* 保留同步前后的完整审计记录。

## 5.4 重启恢复

系统重启后不能从零开始。

恢复流程：

1. 读取数据库中的活动订单；
2. 连接券商；
3. 查询当前资金；
4. 查询当前持仓；
5. 查询当日委托；
6. 查询当日成交；
7. 匹配本地与券商订单；
8. 修复未完成状态；
9. 完成核对；
10. 核对通过后才允许发送新订单。

---

# 6. 第四阶段：最小基础风控

这一阶段只做交易基础设施所必需的风控，不做复杂组合风险模型。

## 6.1 下单前检查

每笔订单至少检查：

* 账户是否连接；
* 行情是否有效；
* 标的是否合法；
* 价格是否合法；
* 数量是否合法；
* 是否满足最小交易单位；
* 买入资金是否足够；
* 卖出可用持仓是否足够；
* 是否超过单笔最大金额；
* 是否超过单标的最大仓位；
* 是否存在重复订单；
* 是否处于禁止交易状态。

## 6.2 系统级限制

初期建议配置：

```yaml
risk:
  max_order_value: 10000
  max_symbol_position_value: 30000
  max_daily_buy_value: 50000
  max_active_orders: 10
  max_orders_per_minute: 5
  allow_short: false
  allow_market_order: false
```

第一阶段宁可限制严格，也不要追求自动化程度。

## 6.3 Kill Switch

系统必须提供：

* 暂停新订单；
* 只允许减仓；
* 撤销全部活动订单；
* 暂停某个策略；
* 暂停某个账户；
* 全局停止交易。

Kill Switch 应当由交易内核执行，不能只依赖网页按钮。

---

# 7. 第五阶段：人工交易控制台

在接入自动策略之前，先构建一个简单的人工控制界面。

功能包括：

* 查看连接状态；
* 查看账户资金；
* 查看持仓；
* 查看活动订单；
* 查看成交；
* 手工发送测试订单；
* 手工撤单；
* 暂停交易；
* 执行持仓核对；
* 查看错误和异常；
* 查看交易日志。

前端只负责发送命令和展示状态。

真正的订单状态、持仓和风控必须由后端交易内核维护。

---

# 8. 第六阶段：接入最简单的自动策略

交易基础设施稳定后，再增加一个非常简单的策略接口。

例如：

```python
class Strategy:
    def on_start(self, context):
        pass

    def on_market_data(self, event, context):
        pass

    def on_order_update(self, event, context):
        pass

    def on_trade(self, event, context):
        pass

    def on_timer(self, event, context):
        pass
```

策略不能直接调用券商接口，而是生成交易意图：

```python
context.submit_order(
    symbol="510300.SH",
    side="BUY",
    quantity=100,
    limit_price=3.85,
)
```

订单仍然必须经过：

```text
Strategy
→ Order Intent
→ Risk Manager
→ Order Manager
→ Broker Adapter
→ Broker API
```

## 第一批测试策略

第一批策略不以收益为目标，而以验证系统为目标：

* 每日固定时间查询账户；
* 定时生成一次小额调仓；
* 固定标的目标仓位策略；
* 简单均线策略；
* 收盘前撤销未完成订单；
* 定时执行持仓核对。

该阶段重点观察：

* 程序能否连续运行；
* 是否发生重复下单；
* 状态是否会丢失；
* 成交和持仓是否一致；
* 网络中断后能否恢复；
* 第二天能否正确继续运行。

---

# 9. 第七阶段：可靠性增强

完成基本实盘后，再系统性提高可靠性。

## 9.1 故障注入测试

主动模拟：

* 交易接口断开；
* 行情断开；
* 请求超时；
* 数据库短暂不可用；
* 程序强制退出；
* 重复成交回报；
* 订单回报乱序；
* 部分成交后重启；
* 撤单请求超时；
* 券商状态与本地状态不一致。

每种故障都应有明确结果：

```text
继续交易
暂停新订单
仅允许减仓
进入人工处理
自动恢复
```

## 9.2 可观测性

需要记录：

* 每次 API 请求；
* 每次 API 返回；
* 每次订单状态变化；
* 每次成交；
* 每次持仓变化；
* 每次风控决策；
* 每次人工操作；
* 每次连接和断开；
* 每次状态恢复。

日志必须能回答：

> 某笔订单为什么产生、经过了什么检查、什么时候发送、券商返回了什么、最后如何影响了持仓和资金。

## 9.3 长期运行测试

进入正式策略开发前，系统应至少经历：

* 多个完整交易日；
* 多次正常启动和关闭；
* 多次网络异常；
* 委托、撤单和部分成交；
* 盘前、盘中、盘后状态切换；
* 人工交易与程序交易同时存在；
* 本地和券商状态核对。

---

# 10. 第八阶段：行情、回测与策略体系

到了这个阶段，交易基础设施才开始服务于“追求收益”。

新增模块：

```text
行情采集
历史数据
回放系统
策略回测
交易成本模型
策略参数管理
策略版本管理
模拟交易
实盘策略部署
策略绩效归因
```

## 10.1 回测和实盘统一接口

策略在回测、模拟和实盘中尽量使用相同接口：

```text
HistoricalDataAdapter
PaperBrokerAdapter
LiveBrokerAdapter
```

策略不应知道当前运行环境是：

* 历史回测；
* 行情回放；
* 模拟交易；
* 实盘交易。

但回测引擎和实盘系统不需要一开始完全相同。

首先保证实盘可靠，再逐步统一事件模型。

## 10.2 策略上线流程

正式策略必须经过：

```text
研究
→ 回测
→ 样本外测试
→ 行情回放
→ 模拟交易
→ 影子交易
→ 小资金实盘
→ 扩大资金
```

---

# 11. 第九阶段：因子系统与收益优化

这一阶段才开始建设丰富的策略研究能力。

包括：

* 因子表达式；
* 因子计算引擎；
* 因子数据缓存；
* 横截面分析；
* IC 和 Rank IC；
* 分层回测；
* 因子相关性；
* 因子中性化；
* 因子组合；
* 组合优化；
* 交易成本和容量分析；
* 多策略资金分配。

“追求高收益率”主要在这一阶段解决，而不是在交易内核阶段解决。

交易内核负责：

* 不丢订单；
* 不重复下单；
* 不错误计算持仓；
* 不绕过风控；
* 不因重启造成错误交易。

研究系统负责：

* 找到有效信号；
* 判断信号是否稳定；
* 管理组合风险；
* 提升扣除成本后的收益。

当前离线研究实现将上述职责固化为：

```text
冻结原始数据 → 因子/特征 → 标准化信号 → 目标仓位
→ 组合与风险贡献硬约束 → 止盈止损/回撤降险 → 10/20/50 万可执行性
→ 研究订单 → 研究成交 → 成交驱动持仓与盈亏 → 可重放报告
```

单资产风险贡献上限不是告警指标：缺少可用协方差、约束数学不可行或求解不收敛时，
研究运行必须在生成订单前失败关闭。该约束只属于离线 `ResearchRun`，不能替代
实盘 `PreTradeChecker`、资金/仓位预占、Kill Switch 或券商侧核对。

---

# 12. 第十阶段：LLM 因子发现

LLM 是整个项目最后接入的研究增强能力。

LLM 可用于：

* 阅读论文和研究报告；
* 提出市场假设；
* 生成因子表达式；
* 解释因子经济逻辑；
* 分析回测失败原因；
* 改进因子参数；
* 发现因子组合；
* 总结市场状态；
* 管理历史研究实验。

LLM 不允许：

* 直接连接实盘账户；
* 直接发送订单；
* 修改账户持仓；
* 绕过风控；
* 在实盘运行时动态生成代码并立即执行；
* 自己决定扩大资金规模。

完整链路应为：

```text
LLM 提出假设
→ 因子表达式
→ 自动检查
→ 回测
→ 样本外测试
→ 人工审批
→ 模拟交易
→ 小资金实盘
```

LLM 只负责提高研究效率，不负责交易可靠性。

---

# 13. 重新调整后的阶段优先级

## P0：确认真实交易接口

* 选择一个市场；
* 选择一个券商或平台；
* 开通程序化权限；
* 获取模拟或实盘账户；
* 验证 API 可以连接。

## P1：交易接口最小封装

* 连接；
* 查询账户；
* 查询资金；
* 查询持仓；
* 查询委托；
* 查询成交；
* 下单；
* 撤单；
* 接收回报。

## P2：订单和持仓管理

* 本地订单 ID；
* 订单状态机；
* 成交处理；
* 持仓计算；
* 账户快照；
* 数据库存储；
* 重启恢复；
* 券商状态核对。

## P3：基础风控和人工控制

* 下单前检查；
* 仓位限制；
* 资金限制；
* 订单速率限制；
* Kill Switch；
* 人工交易控制台。

## P4：连续实盘运行

* 简单测试策略；
* 网络异常恢复；
* 故障注入；
* 日终核对；
* 长期稳定性测试。

## P5：策略框架

* 统一策略接口；
* 行情事件；
* 定时事件；
* 目标仓位；
* 调仓执行；
* 策略状态持久化。

## P6：回测与研究

* 历史行情；
* 回测；
* 模拟交易；
* 成本模型；
* 策略评估。

## P7：因子和组合

* 因子计算；
* 因子评测；
* 多因子模型；
* 策略组合；
* 风险预算。

## P8：LLM 研究 Agent

* 因子假设生成；
* 因子表达式生成；
* 自动实验；
* 结果分析；
* 研究记忆。

---

# 14. 第一版最小可用产品

第一版 MVP 的边界应严格控制。

## 包含

* 单交易账户；
* 单券商接口；
* 股票或期货中的一个市场；
* 账户查询；
* 资金查询；
* 持仓查询；
* 委托查询；
* 成交查询；
* 限价下单；
* 撤单；
* 订单回报；
* 成交回报；
* 本地订单状态机；
* 本地持仓管理；
* PostgreSQL 持久化；
* 重启恢复；
* 持仓核对；
* 基础风控；
* 简单管理页面；
* 手工测试策略。

## 不包含

* LLM；
* 因子研究；
* 多因子模型；
* 复杂回测；
* 多券商；
* 多账户；
* 分布式微服务；
* 高频交易；
* 智能拆单；
* 自动策略生成；
* 在线模型训练；
* 自动扩大仓位。

---

# 15. 第一版成功标准

项目第一版是否成功，不看收益率，而看以下指标：

* 可以从自己的程序发送真实订单；
* 可以正确撤单；
* 可以收到委托和成交回报；
* 可以正确查询资金和持仓；
* 本地持仓和券商持仓一致；
* 程序重启后不会重复下单；
* 网络断开恢复后订单状态正确；
* 外部手工交易后能够发现持仓差异；
* 任何订单都能追溯其完整生命周期；
* 可以随时暂停新订单；
* 可以连续运行多个完整交易日；
* 即使策略出错，也不能绕过交易风控。

第一版完成后，这个项目才真正拥有一个可以承载后续策略、因子和 LLM 研究的可靠基础。
