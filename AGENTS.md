# AGENTS.md

可实盘交易的量化系统。实盘交易涉及真实资金，改动须谨慎，不要在未确认风险的情况下改动下单 / 持仓 / 风控相关逻辑。

## 命名约定

- **全名**:`FinDashboard`(大写 F、D、S)—— 用于文档标题、对外说明、正式表述。
- **缩写**:`finboard`(全小写)—— 用于包目录名(`packages/finboard-*`)、Python 模块名(`finboard_*`)、CLI 命令(`finboard`)、内部叙述。
- **禁止变体**:`Findashboard`、`FINBOARD`(全大写,除非是环境变量前缀 `FINBOARD_`)、`Finboard`(仅首字母大写)等。
- **历史遗留标识符(保持不变,不视作违规)**:
  - `pyproject.toml` 的 `name = "findashboard"`(分发包名,改影响大);
  - PostgreSQL 默认库名 / 用户名 `findashboard`(用户已按此建库,见 `README.md` 数据库初始化);
  - 前端 `web/package.json` 的 `name = "findashboard-web"`、localStorage key `findashboard-theme`(已发布的标识符)。
  这些是技术标识符,不是叙述用词;新代码 / 新文档应遵循上方的全名 / 缩写约定。

## 项目设计

完整设计文档见 `phase1_doc.md`（生产级量化交易系统开发计划）。以下为对 agent 最关键的红线与约束。

### 架构选型（第一阶段硬约束）
- 技术栈：Python 交易进程 + PostgreSQL
- 形态：**模块化单体**；不引入微服务 / Kafka / Kubernetes / 分布式事务
- 范围：单账户、单券商接口、单市场（A股或期货二选一）；在链路稳定前不抽象多券商适配层
- 模块划分：Broker Adapter / Trading Gateway / Order Manager / Position Manager / Account Manager / Risk Manager / Strategy Runner / Reconciliation

### 阶段优先级
- **P0/P1（已完成）**：端到端真实交易链路（MockBroker + QMT 适配器）—— 查询账户 / 发单 / 撤单 / 接收回报 / 持仓核对 / 重启恢复。issue #1-#5。
- **P2（已完成）**：重启恢复完善（UNKNOWN 状态修复 + 本地↔券商订单匹配）—— issue #9。
- **P3（已完成）**：基础风控 + 人工交易控制台（PreTradeChecker / KillSwitch / FastAPI + React）—— issue #12。
- **P4（已完成）**：连续实盘运行基础设施 —— 故障注入测试 + 可观测性增强（#13）、定时任务调度（#14）。
- **P5（大部分完成）**：策略框架 + 行情事件 —— 策略运行器（#11）、行情数据接入（#10）已实现；策略状态持久化待验证。
- **当前方向：回测 + 策略开发** —— QMT 权限受阻,实盘验证暂缓（#22/#24/#26）。转向历史数据源（#27 akshare/tushare）、回测引擎（#28 行情回放 + 纸面撮合 + 绩效分析）、第一个信号驱动策略（#29 均线交叉）。
- **研究里程碑收尾（#66/#77-#81/#83/#91）**：研究数据发布、因子实验室、无代码策略规格、统一 `ResearchRun` 生命周期、正式组合风控流水线和持久化产品模拟盘均已建立；研究运行使用独立表与 `RR-` ID，模拟运行使用 `simulation_*` 表与 `SIM-*` ID，均不得写入实盘订单/成交/持仓。前端暂不提供 Python 策略代码。
- **多期再平衡回放（#183，措辞经 #203 澄清）**：research_run 支持 `parameters.rebalance_frequency`（monthly|quarterly）从冻结发布交易日历推导每期 decision date，每期重算 universe/features/signals 与组合，决策间每日 mark-to-market 产出全区间权益曲线与绩效指标（总收益/年化/夏普/最大回撤）；返回与 report 标注 `execution_mode`（single_shot|multi_period）。**multi_period 必须显式声明 `rebalance_frequency`**；「多期不要求预建因子快照」仅限决策日推导与价格因子（momentum/volatility 等按发布每期重算），基本面因子（pb/ROE 等）仍 PIT 取自冻结快照/研究数据发布（#187）。未声明即 single_shot，该路径决策时点只能来自冻结因子快照（fail-closed，见 #203 入队校验）；纯离线研究域，不连 broker 不下单。
- **指数基准行情（#184）**：akshare 指数日线（`000300.SH` 等）按代码规则分流 `index_zh_a_hist`，指数 bar 可缓存、可发布（INDEX 放行发布通道，`mult_asset_mixed` 发布至少含 stock/etf/index 之一，指数不可撮合只做基准数据）。事件驱动回测：MCP `finboard_backtest_run` 支持 `benchmark_symbol`、REST `BacktestRunRequest.benchmark`，透传 `BacktestConfig`，引擎在基准不在 universe 时单独拉取基准 bars。research_run 管线按 `benchmark_config.symbol` 从冻结发布取行情计算真实 `benchmark_return`/`excess_return`（不再读手工 `overrides.return` 默认 0.0）。**基准缺失时两条路径均返回 `benchmark_return=null` + 具名 warning，禁止静默 0.0**；`backtest_runs.benchmark_config` 如实归档。**回退链跟随选股池（#254）**：显式标的 > 每期选股池动态等权（选股启用且存在 published 快照时，等权日收益按当期 `selected_symbols` 平均，空选股期持币持平）> 静态候选池等权 > 首个标的——选股启用时回退基准不再使用静态 cfg.symbols 全池（旧口径几乎必然不在当日候选池内）；实际来源记入 `BacktestResult.benchmark_source`（REST `BacktestMetricsOut.benchmark_source` / MCP metrics 同名键 / `summary()` 基准来源行）并打 `backtest.benchmark_source` 日志。纯离线研究域，不连 broker 不下单。
- **指数标的数据链路入口（#256，#184 能力运营化）**：#184 读取端完整但全链路无 `instrument_type=index` 登记写入者，000300.SH 进不了缓存与冻结发布、`benchmark_return` 恒 null。四断点闭合：（1）**登记**——`UniverseDiscovery.discover_indices` 从受控登记表 `BENCHMARK_INDEX_REGISTRY`（沪深300/中证500/中证1000/上证50/科创50/创业板指/深证成指/北证50 等 9 只，代码必须满足 `is_index_code` 规则且导入期断言防漂移）产出指数 `InstrumentInfo`，并入 `discover_all()`，data_sync 经 `sync_with_diff` 自动登记；指数无 list_date/industry 结构化上游，保持 null 可见缺失、不虚构元数据。（2）**同步**——`bulk_download` 的 `instrument_type=index` 从 instruments 表筛指数走 akshare 指数日线（缓存键与其他资产同为 `qfq`，发布 adjustment 必须与下载键一致，默认均 qfq）；tushare 源 scope 拒绝行为保持（`tushare_scope_mismatch` 不静默换源）。（3）**发布**——manifest 只允许一个 bars 主发布，基准行情必须与候选池**同处一份 multi_asset_mixed 发布**（把指数代码一并放进发布 symbols）。（4）**候选池边界**——指数只做基准数据、不可撮合（#184 既有边界落到实现）：`universe_precheck.static_universe_candidates` 与 `frozen_loader._build_candidates_and_lots` 共用 `is_benchmark_only_instrument` 谓词把指数排除出候选枚举（静态预检/入队空池门控/运行时候选三处口径一致），元数据缺失 warning 统计同步跳过指数（否则每个含基准指数的混发发布永久产出 list_date 噪音 warning）；指数 bars 仍可经 `_load_benchmark_curve` 单独读取计算基准收益。端到端集成测试 `tests/integration/test_index_benchmark_chain.py`：登记→混发发布→真实 FrozenReleaseProvider worker run → `benchmark_return` 非 null + UNIVERSE artifact 无指数。运营步骤见 `docs/research/data-ops.md`。纯离线数据/研究域，不连 broker 不下单。
- **ETF 行情接口分支与缓存多源策略（#257）**：ETF 轮动 / 均值回归策略空转的两个数据断点闭合。**复现结论**（akshare 1.18.78 实测）：股票日线接口内部按 `6` 开头判定沪市，SH-ETF 被拼成深市 secid（`stock_zh_a_hist("510300")` 请求 `secid=0.510300`），正确路由是 `fund_etf_hist_em`（`get_market_id` 正确处理 5 开头沪市基金）；本仓无任何 secid 拼接代码，错误归属接口误用。（1）**取数**——`AkShareProvider` 按代码规则新增 `is_etf_code` 谓词（`5xxxxx.SH` / `15|16xxxx.SZ`，与 `is_index_code` 同风格，代码段互不重叠），ETF/LOF 日线走 `fund_etf_hist_em`、分钟线走 `fund_etf_hist_min_em`，缓存键/adjust 映射/列名映射与股票分支一致。（2）**缓存多源**——`TushareBarProvider` 不再静默丢弃异源缓存：异源（akshare/yfinance/mixed）缓存完整覆盖请求区间时 read-through 直接返回（具名 `tushare.foreign_cache_hit`，零 tushare 预算消耗）；缺口区间 tushare 拉不到（ETF/指数等 2000 积分不覆盖标的）时具名回退返回异源缓存（`tushare.foreign_cache_fallback`）；缺口区间拉得到（股票）时保持既有「丢弃重建纯 tushare 缓存」语义——避免两种复权口径混进同一条权益曲线，混合拼接不在缓存层发生。（3）**回测回退链**——新增 `finboard_data.FallbackBarProvider`（primary 空结果/异常时回退备用源，只代理引擎唯一消费入口 `fetch_bars`）与 `finboard_backtest.providers.build_backtest_bar_provider`（统一构造 + 按 `settings.data_fallback_provider` 包裹），接入 REST/`backtest_run` 执行器（`run_backtest_and_persist`）与 MCP 同步路径两个构造点，三入口（REST 入队/MCP 同步/MCP 异步入队）语义一致；默认关闭（settings 为 None/未配置/与主源同名均不包裹），回退只解决「主源整体不覆盖标的」，部分区间缺失由缓存层策略承担。端到端集成测试 `tests/integration/test_etf_bar_chain.py`（mock akshare 同步 → 缓存 → tushare 源引擎回测出成交）。运营步骤见 `docs/research/data-ops.md`。纯离线数据域，不连 broker 不下单。
- **可转债数据链路（#265）**：可转债双低策略（#63）的数据上游闭合，四断点。（1）**登记**——`UniverseDiscovery.discover_convertibles` 从东财可转债一览 `bond_zh_cov`（列名容错解析 `parse_convertible_overview_frame`，纯函数可离线测）产出 `instrument_type=convertible` 标的（`is_convertible_code`：`11xxxx.SH`/`12xxxx.SZ`，与 `is_index_code`/`is_etf_code` 同风格；北交所无场内转债不纳入；6 位代码归一必须走 `normalize_overview_code`——`normalize_a_share_code` 的 6 位数字规则不认 1 开头段），并入 `discover_all()`，data_sync 经 `sync_with_diff` 自动登记；东财一览只覆盖存续转债（存续偏差已知，cb_basic 摘牌档案缓解），无 list_date 保持 null。（2）**行情**——`TushareBarProvider` 按代码规则分流 2000 积分档 `cb_daily`（无复权概念，不调 adj_factor；缓存键沿用请求 adjust 语义 no-op，发布 adjustment 与下载键一致；1 手=10 张 vol ×10）；`bulk_download` 的 convertible 放行 tushare 源（`tushare_scope_mismatch` 对其余非股票标的保持），akshare 源转债日线 fail-visible 拒绝（股票接口会把 1 开头误路由，#257 同源缺陷）。akshare 兜底接口：`bond_zh_cov`（东财一览，评级 + 交叉验证）与 `bond_cb_redeem_jsl`（集思录强赎），限流/退避走 provider 既有约定。（3）**条款元数据**——`research_data_sync` 新增 dataset `convertible_profiles`（默认全数据集之一）：tushare `cb_basic` 字段白名单（`_CB_BASIC_FIELDS`，cb_basic 无评级字段）在市 L+摘牌 D 合并，快照 upsert 主数据 `convertible_metadata`（`ConvertibleMetadataRepository.upsert_many`：快照刷新语义——上游非 null 才覆盖，`conversion_price NOT NULL` 缺价行跳过计数），顺带回填 `instruments.list_date/delist_date`（`backfill_listing_dates` 只补 null）并导入强赎事件（`import_lifecycle_events` 按 (symbol,event_type,effective_date,source,dataset_version) 幂等，`jsl_redeem_v1` 版本恒定；未来生效的赎回按生效日可见——领域不变量 `available_at >= effective_date` 开盘，真实观察时间留 `details.observed_at`）；评级/强赎兜底失败降级为 warning 不阻断 tushare 主链路。#265 迁移：`convertible_metadata` 新增 `rating` 列（可空快照）。（4）**发布与消费**——`ReleaseDatasetKind` 新增 `convertible_metrics`（发布执行时从缓存 bars × 冻结转股价元数据计算转股价值 `100/转股价×正股收盘` 与转股溢价率 `转债收盘/转股价值-1`，逐日带日期观测 `available_at`=T 日 15:30 上海；只接受 A 股转债 `convertible_scope_violation`，元数据缺失 `convertible_metadata_missing`，整期无正股收盘 `no_underlying_close_for_premium` 拒绝，部分缺口 `premium_missing_days` 可见）；发布 instruments 携带 `convertible` 条款快照（manifest 新增 `convertible` 键，非转债省略键 checksum 不漂移），`FrozenReleaseProvider.fetch_convertible_metrics` PIT 门控读取，`RESEARCH_RELEASE_FEATURE_NAMES` 注册 `conversion_premium`/`conversion_value`；转债条款事件要求降级为不设硬门（v1 上游凑不齐 #58 四类条款事件，逐券全事件才 ready 会让真实发布永不可发布；已同步事件仍随 manifest 冻结，质量报告 `convertible_instruments` 块可见转债标的数/with_metadata/missing_maturity_date/missing_rating/with_lifecycle_events）；mixed 发布展开并入 convertible（`_FULL_MARKET_MIXED_TYPES`）。**转股溢价率 PIT 语义（诚实边界）**：cb_basic 是当前时点快照，不含转股价下修史/除权除息调整史，派生观测只能宣称「冻结快照转股价 × 同日正股收盘」的带日期冻结语义，不得宣称全历史 PIT；集思录强赎无历史公告时间（历史公告回补需 5000 积分 `cb_call`，后续 issue）。E2E：`tests/integration/test_convertible_chain.py`（mock bond_zh_cov/cb_daily/cb_basic → 登记 → 缓存 → convertible_profiles 同步 → 双发布 → `convertible_double_low` 2024-2026 合成数据非空成交）。**已知缺口（本 issue 不修）**：通用事件驱动引擎默认 resolver 把一切代码按 A 股股票撮合（engine.py 不传 resolver），转债走通用引擎需 resolver 注入；`convertible_double_low` 独立模拟器路径不受影响。运营步骤见 `docs/research/data-ops.md`。纯离线研究数据域，不连 broker 不下单。

- **期货 EOD 数据接入（#267，路线 C 市场中性对冲的数据面前置）**：范围只做数据面（期货 EOD 入缓存 / 可发布 / 可做研究基准数据）；「股票多头 + 股指空头」对冲组合回测工程（换月展期 / 贴水成本 / 保证金占用）与实盘单市场硬约束议题另行立项。（1）**登记**——`finboard_data` 受控登记表 `FUTURES_MAIN_SERIES_REGISTRY`（#256 `BENCHMARK_INDEX_REGISTRY` 同风格，导入期断言防漂移：代码必须满足 `is_futures_main_code` 且 `product` 与代码一致）登记 **IF/IH/IC/IM 主连**（CFFEX 股指期货优先，路线 C 空头腿；乘数/保证金率与 finboard-backtest `FuturesRule` 同口径，跨包单测锁定），`UniverseDiscovery.discover_futures_main`（无网络调用）产出 `market=future`/`instrument_type=futures` 标的并入 `discover_all()`，data_sync 经 `sync_with_diff` 自动登记；**登记语义是主连序列（continuous）**，具体月份合约不经本入口登记（v1 无结构化上游，合约链另行立项）；主连无 list_date 上游保持 null 可见缺失；未登记品种 `futures_series_entry` fail-closed 拒绝（不猜测乘数）。（2）**行情**——`AkShareProvider` 主连日线走新浪 `futures_main_sina` 入缓存（无复权概念，缓存键沿用请求 adjust 语义 no-op、发布 adjustment 与下载键一致，#256/#265 同策略；新浪无成交额列 amount=0；成交量按手 1:1 落盘）；**主连 vs 具体合约语义显式标注不混淆**——非主连代码在缓存层 fail-visible 拒绝（具体合约 EOD 走 `fetch_futures_official_daily` 交易所官网按日全市场表 `get_futures_daily`，v1 不进逐标的缓存），tushare 源对期货 fail-visible 指路 akshare（fut_daily 属另档积分，不静默走股票 daily 误路由），`bulk_download` 的 tushare scope 拒绝并提示另建 akshare 任务；`cache.make_symbol` 补期货交易所后缀（.CFFEX/.SHFE/.DCE/.CZCE/.INE/.GFEX）→ `Market.FUTURE` 推断（此前未知后缀一律兜底 A_SHARE 会让冻结发布逐标的 market 校验误拒），与 `assets.registry._PREFIX_TABLE` 同口径（单测锁定）。（3）**发布与消费**——BARS 发布（source=akshare）按 `_futures_main_candidate` 构建期货候选（乘数/保证金率/最小变动价位/`allows_short` 从登记表读取），mixed 发布展开并入 futures（`_FULL_MARKET_MIXED_TYPES` 五类：stock/etf/index/convertible/futures）；期货事件硬门降级（同 #265 转债决策：#58 换月/到期/交割事件在主连日线上无结构化上游、主连本身无「单份合约到期」语义，逐合约全事件才 ready 会让真实发布永不可发布；已同步事件仍随 manifest 冻结，质量报告 `futures_instruments` 块可见 total/continuous/missing_list_date/with_lifecycle_events）；`is_benchmark_only_instrument` 扩为 index+futures——期货主连只做基准/研究数据、不可撮合（#184 指数先例），静态预检与运行时候选共用谓词口径一致。E2E：`tests/integration/test_futures_chain.py`（受控登记 → mock futures_main_sina → 缓存 → BARS 发布 → 真实 `FrozenReleaseProvider` 读回；主连/合约语义守卫）。运营步骤见 `docs/research/data-ops.md`。纯离线数据域，不触实盘单市场硬约束，不连 broker 不下单。

- **instrument 元数据回填（#185，#251 修复回填断点并补齐上游）**：akshare 发现链路只写 `instruments` 的 code/name/market/instrument_type/exchange/listing_board，list_date/industry 无写入者；tushare `stock_basic` 的 list_date+industry+delist_date 落在 `research_instrument_profiles`。data_sync 后置回填批次口径为「最近一次**实际摄取过档案的批次**」（#251：不再要求已发布——profiles 摄取过但从未发布时旧逻辑永久短路、元数据全空；发布构造器兜底仍保持已发布口径不变），只回填 null，不覆盖已有主数据；#251 起回填含 delist_date（来自退市档案 list_status=D，`research_data_sync` 的 profiles 段同时拉取 L+D 合并进同一批次），`status` 不随回填变更（缺席二次确认是退市状态的唯一权威路径）；发布构造器在 instruments 字段为 null 时从 profiles 兜底（含 delist_date），release instruments 携带 list_date/industry（manifest 新增 `industry` 字段）；sync/发布任务报告缺失字段统计（`quality_report.instrument_metadata` 含 missing_list_date/missing_industry/missing_delist_date/name_history_coverage / job phase），缺失可见而非静默。#251 新增 `research_data_sync` dataset `name_changes`：tushare `namechange` 分页拉全，以半开区间重建 `instrument_names`（未提及 symbol 保留既有记录、重跑幂等），补齐 #213 ST-PIT 的历史名称上游；`sector` 尚无上游来源，保持 null。纯离线数据域，不连 broker 不下单。
- **universe 预检与候选池诊断（#186）**：`strategy_validate`（REST+MCP）返回 `universe_precheck`——对主数据发布的 instruments 做静态评估，检查 universe 过滤条件依赖字段（list_date / delist_date / average_amount / ST 标记 / required_data_fields / ranking.field）存在性，缺失返回具名 warning，并给出候选池空池预览（total/included/排除统计 `excluded_by_condition`/缺失字段 `missing_fields`）；`run_queue`（REST+MCP，含 backtest_run 的 strategy_spec 形态）入队同步做候选池非空校验，空池秒级 `invalid_argument`（附排除统计与缺失字段名），杜绝「list_date 全 null → listing_days=0 → 全排除」后等执行期跑 30+ 分钟才报泛化错误；signal_engine 执行期对「因缺元数据未生效」的过滤发出运行时降级 warning，空池错误附根因字段。价格/特征类字段（min_price 等）运行时恒有，静态预览不误报空池。**评估域跟随 explicit_symbols（#254）**：声明 `universe.explicit_symbols` 时静态预检与执行期过滤/警告的评估域收窄为 explicit ∩ 发布标的（静态与运行时共用 `explicit_symbol_domain`，两边一致），不再对全市场产出 `not_in_explicit_symbols` 海量噪音；声明但发布中缺失的标的发具名 warning `universe_explicit_symbol_missing`（全部缺失判空池秒级拒绝）；multi_period 每期价格特征重算同步按声明域收窄（`build_price_feature_snapshot(symbols=...)`），行为不变、成本从发布全市场降到声明规模。纯离线研究域，不连 broker 不下单。
- **research_db 选股数据集发布门（#255，runs 273-275 静默 0 交易修复）**：v1 选股 `inputs_mode=research_db`（默认）必需的数据集批次（`instrument_profiles` 恒必选，按 required_factors 再加 `daily_metrics`/`financial_indicators`）**从未发布**时，旧链路逐期 SKIPPED、引擎 0 交易「成功」收场。三层防线共用 `ResearchDatasetRepository.selection_inputs_gate`（粗粒度＝是否存在已发布批次，`dataset_versions` 声明时精确匹配版本）：（1）入队期——REST `POST /api/backtest/run` 与 MCP `finboard_backtest_run`（同步 + `_enqueue_backtest_job` 异步）秒级 422/`invalid_argument`（`dataset_unpublished:{dataset}` 具名，附修复路径）；（2）执行端——`run_backtest_and_persist` 重放同一检查，失败 `selection_dataset_unpublished`（不重试），覆盖 grid 提交等旁路；（3）引擎——选股启用的 run 一律携带 `selection_diagnostics`（published/skipped 快照数、skip 原因计数、候选池是否曾生效），整期无候选打 `backtest.selection_pool_never_active` warning 并在 `summary()`/REST `BacktestMetricsOut.selection_diagnostics`/MCP metrics 同名键可见。bars/snapshot 模式不拦截（缺失按 #173 设计降级）。运营步骤见 `docs/research/data-ops.md`（research_data_sync 质量门通过即自动 mark_published；摄取 ≠ 发布）。纯研究/回测域，不触交易安全红线。
- **冻结发布多数据集联合发布（#187）**：`dataset_release_publish`（REST+MCP）的 `release_kind` 扩为 4 值——在 `a_share_tushare` / `multi_asset_mixed` 之上新增 `daily_metrics` / `financial_indicators`：从 `research_*` 表（research_data_sync 摄取，`source=tushare` 且 `adjustment=none`）按字段白名单冻结 parquet（每标的分 kind 目录），质量门为覆盖率 + 元数据完整（`all_null_fields` 仅 as 可见 warning，财务指标稀疏是常态），同名 dataset 字段变化递增 `schema_version`，`research_*` 记录的 `available_at`/`observed_at`/`source` 完整归档。联合消费：因子快照可引用 bars 主发布 + research 发布联合集，产出 pb / 市值 / 换手 / ROE / 毛利率等基本面因子（`build_cross_section_feature_snapshot_from_releases`，`market_cap` 已注册为 `FACTOR_LAB_CATALOG` 的 `signal_eligible=False` 规模特征）；`strategy_validate` / `run_queue` / frozen_loader 的 `dataset_release_ids` 可同时引用多份发布，universe 预检与候选池始终落在 **bars 主发布**（研究数据发布只提供因子观测）；`FrozenReleaseProvider.fetch_daily_metrics` / `fetch_financial_indicators` 按 `available_at <= decision_at` PIT 门控读取。**跨发布标的集一致性（#252）**：发布执行器支持 `consistency_baseline_release_id`（+ `consistency_fail_on_mismatch`，默认 false）对基线发布（如同区间 bars 主发布）做标的集并集差集校验——差集具名（缺哪只、在哪个方向），默认 warning 不阻断、true 时秒级 `symbol_set_mismatch` 拒绝发布；REST `GET /api/instruments/datasets/releases/{id}/symbol-diff` 与 MCP `finboard_dataset_release_diff`（工具总数 126）提供发布后自检（计数精确 + 差集有界预览）；执行期语义对齐 factor_lab #212 容忍路线——research_run 研究发布缺标的不再 fail-closed 炸整条 run，缺失标的的研究因子值为 null 并发具名 `research_release_missing_symbols` warning（release_id + 缺失清单进 `LoadedDecisionContext.research_release_missing_symbols`），bars 主发布缺标的仍 fail-closed。纯离线研究域，不连 broker 不下单。
- **research_run 分阶段进度上报（#188）**：`ResearchRunCoordinator.execute` 新增可选 `progress` 钩子，在 `_DECISION_STAGES` 逐 13 个 stage 与 REPORT 段回调 `progress(done, total, "research_run:<stage>")`，**不改变 artifact/checkpoint 语义**；决策总数 N 执行期才得知，total 采用「随新决策被发现而递增」的自修正计数（单快照 total=13 精确），worker 侧 `update_progress` 对 total 只增不减与之兼容；进度回调经 `_report_progress` 尽力而为（异常吞掉但 `CancelledError` 照常透传），上报故障不改变运行状态机。运行中 `finboard_job_get` / `GET /api/jobs/{id}` 可见 `research_run:<stage>` phase 与逐段 progress_done/total，可区分「正常计算」与「卡死」；终态 phase 保持 `research_run:<status>` 兼容。纯离线研究域，不连 broker 不下单。
- **research_run 失败摘要附阶段上下文（#263，#188 精神的可观测性补全）**：通用异常收口的 `error_summary` 头部统一拼结构化定位头 `[stage=...; decision=...; decision_index=...（1-based）; release=<bars 主发布>; dataset_releases=...; factor_snapshots=...]`，原始异常消息保留在尾部——`error_code` 保持异常类型名不变，四个专项分支（unsupported / hard_constraint / interrupted / non_deterministic_replay）的 `error_summary` 保持 `str(exc)` 原样，fail-closed 状态机与 #188 进度语义零改动。两层协作：数据加载期（multi_period 全部期次一次性预构建，失败发生在任何 artifact 落库之前）由 `signal_engine.build_decision_load_contexts` 逐期循环在 bare raise 前给异常对象挂标记（异常类型/消息/traceback 不变，既有具名文案与 `pytest.raises` 断言零破坏），runner 经 `read_decision_load_context` 读回失败期次精确决策日与 bars 主发布；决策执行期由 runner 循环维护 `FailureContext`（stage 依 `decision_load` → `decision_execute` → 具体 stage 值 → `report` 推进）。worker 兜底 1000 字符截断改为**保头保尾**（`truncate_summary`：头部 ~846 + 省略标记 + 尾部 ~140，应用于 job_crash 兜底与适配器工厂失败两处既有 `[:1000]`），超长错误截断后 stage + 决策日仍可读。排障顺序：先读 `finboard_run_get` 的 error_summary 头部定位 stage/决策期/发布，再按需下钻 artifacts。纯可观测性，无迁移无 schema 变更，不触交易安全红线，不连 broker 不下单。
- **研究/回测 job 级耗时打点（#285，并行化系列 #286-#288 的量化前置）**：回答「慢在 IO 还是计算、慢在加载还是逐决策执行」。（1）回测引擎——`BacktestEngine.run()` 内部计总耗时（`total_elapsed_seconds`）与数据加载段耗时（`data_load_elapsed_seconds`，含显式基准拉取），随 `BacktestResult.timing` 进 `backtest_runs.metrics` JSON（REST history / MCP `backtest_run`/`backtest_history_get`/`report_backtest` 自然可见）与 `backtest.timing` structlog 事件（无数据早退路径同样记录）。（2）research_run——`ResearchRunCoordinator.execute` 在阶段边界计 `decision_load` 总耗时（`async for` 等价展开为 `__anext__` 手工迭代，产出下一条决策的耗时计入加载）、逐决策 execute 耗时聚合（`decision_execute` = count/min/avg/max + `slowest_decision_date` 定位最慢决策日）与 report 段耗时，随 `save_result(timing=...)` 存入 `research_runs.result` JSON 的 `timing` 键（record 级字段，不进 report、不参与 result_checksum——重放耗时不同不判漂移）并打 `research_run.timing` structlog 事件。（3）parquet 读取按 job 聚合——`ParquetCache` 读取入口（`read`/`read_close_points`/`metadata`）经 contextvar 句柄 `collect_parquet_read_stats()` 在**入口处计数**（read_ops / read_elapsed_ms / read_bytes / ops_by_entry，footer-only 读取按 io_stats 同口径上限 64KB 计字节），不动读取内部逻辑、保留既有逐次 elapsed_ms debug 日志；engine 把聚合包在数据加载段外、research_run 包在决策循环外，未激活零开销。纯可观测性：不改 fail-closed 状态机 / checkpoint / checksum 语义，#188 进度上报零改动，JSON 列自然携带无迁移。
- **research_run 加载路径去重（#287）**：multi_period 慢的大头在加载而非计算——三层只读去重，零语义变化（确定性重放 result_checksum / 具名 warning / #263 失败标记 / #188 进度计数不变，均有等值单测锁定）。（1）**provider memoize**：`build_signal_engine_adapter_factory` 的工厂按 release_id 在 run 闭包内缓存实例，逐文件 SHA256 校验缓存（`_verified`）与 manifest 加载每发布只付一次（此前 loader 每决策 3 处调用工厂被打穿）；（2）**进程内读缓存**：`ParquetCache` 实例作用域 LRU（键 = 路径 + size + mtime_ns，write/merge 后失效，元素上限默认 2e6 可配 `read_cache_max_elements`，0 关闭；命中不计 `io_stats` 的真实磁盘 I/O 口径——akshare/yfinance 下载统计依赖；`read`/`read_close_points` debug 日志带 `cache_hit` 字段；`read_close_points` 未命中时整文件点位进缓存、区间内存裁剪）；`read` 公共签名与入口行为不变（兼容并行分支 #285 在入口加计时包裹层）；`read_bars_sync` 改按列 `to_pylist` 提取（缺列/空值语义不变）；（3）**close 矩阵**：`FrozenInputLoader` 实例（= 单 run 装载）首个决策对全部候选各做一次发布全区间 PIT 读取（`available_at`/日期构建期校验非递减），其后 decision/execution close 与价格序列均由前缀切片取得，与逐期 PIT 过滤读取逐值相等；只对真实 `FrozenReleaseProvider` 启用（available_at 由发布元数据确定性派生），测试 stub 与非单调标的回退逐期读取。逐 symbol 串行 await（close 回退 / research features / price series / 权益曲线 close map）全部改 `asyncio.gather` + 信号量，结果与异常按输入顺序组装/抛出，与串行逐值一致；交易日历按 provider 进程内缓存（WeakKeyDictionary，条目随 provider 回收，不跨 run / 跨事件循环）。粗测对照：36 期 × 100 标的加载 109s → 22s（≈5x）。纯研究/回测读路径，不连 broker 不下单，回滚 = revert。
- **worker CPU 密集段 to_thread 卸载 + `--workers N` 多进程（#286，并行化系列之一）**：（1）**to_thread 卸载**——「同步实现被协程直接 await」的 CPU 密集段不再阻塞事件循环线程：回测引擎逐日回放经 `asyncio.to_thread(self._replay_one_day)` 卸载（调用点整体包裹 `engine.run()` 不可行——其内部含真 async 数据加载与 AsyncSession 调用，包裹到线程需嵌套事件循环，跨循环 await 即错），段内 async 调用经 `_EventLoopBridge` 分流：纯计算协程（策略回调 / BacktestBroker 查询 / 成交事件排水，均为同步体）在工作线程 `send` 内联驱动到完成，发生真实 await 或依赖运行中循环的协程（因子选股 DB 读取）放弃半执行副本、投递回事件循环线程重跑——不变式：**重跑只对「首个真实 await 前无外部副作用」的协程安全**（当前回放段内全部满足；新增回放路径协程若在 await 前改外部状态即违反此约定）；数据 IO 段（真 async）不包，#285 parquet 计时 / timing 语义零改动。research_run 侧协方差估计 / 信号求值（`build_decision_load_contexts` / `build_decision_inputs`）、逐决策组合构建（`PortfolioPipelineAdapter._build_decision`）、报告构建（`adapter.build_report`）与 factor_lab 每标的特征观测计算同样卸载。（2）**`--workers N` 多进程**——settings `worker_processes`（默认 1 零回归）+ CLI `--workers`：>1 时父进程**只做监管不领取任务**，以 subprocess 重新拉起 N 个 CLI 单 worker 形态子进程（与 `finboard dev` 托管 worker 同入口，spawn 天然安全无 pickle 约束，不引消息队列），各子进程独立 engine / session_maker / `worker_id`；显式 CLI 覆盖项转发子进程、`--workers` 永不转发（防递归进程树）；Ctrl-C 先等子进程自行收敛、10s 宽限后 terminate/kill 兜底（主动停止退出码 0），子进程崩溃不自动重启（lease 回收后重排，重启安全）。（3）**claim_next 并发正确性**（多消费者的前提，实测复现两处缺陷后修复）：并发 claimer 在彼此提交前互相看不到对方 running 行，双双通过 `max_per_kind` 计数检查各领一个同 kind 任务（validation_experiment 重复揭盲 / research_code_run 双容器风险）——临界区前取事务级 `pg_advisory_xact_lock`（PostgreSQL only，claim→checkpoint 毫秒级窗口，无可感知吞吐影响）串行化；`limit>1` 单次批量内同 kind 多行在同一次计数之后被领取同样击穿上限——改逐行准入（预计数 + 本批已领数 ≥ cap 的行留在队列不置 running）。（4）单测锁定：回放期间事件循环短超时操作不饿死、独立循环 reclaim actor（模拟多进程下的进程 B 维护循环）下卸载后心跳持续续约不被回收 / 不卸载负面对照必然 interrupted（注意：Windows 多线程 GIL 争用下循环侧唤醒延迟实测可达 ~0.4s 且不随 lease 等比缩小，测试 lease 不得缩得太紧，生产 30s/600s 不受影响）、双真实 worker 子进程竞争 40 任务全部 succeeded/attempt=1、`--workers 2` supervisor 端到端。回滚 = `--workers 1`（默认）+ revert；纯研究/回测执行基础设施，不连 broker 不下单。（5）**supervisor 优雅停止同时响应 SIGTERM（#316）**：POSIX 上 supervisor 原先只处理 Ctrl-C（SIGINT），`worker run --workers N` 收到 SIGTERM（systemd / docker stop 默认信号）即刻死亡、N 个子 worker（各自独立会话）孤儿化继续消费队列——CI 集成测试因此被泄漏子进程污染（`test_supervisor_workers_flag_end_to_end` 每跑一次泄漏 2 个幽灵，抢走后续 worker 测试的队列任务，症状为 run 停留 queued / job 已 running 的状态冲突，受害者随竞态逐 run 漂移）；现 SIGTERM 与 Ctrl-C 走同一条优雅收敛路径（signal handler 转 KeyboardInterrupt），子 worker 全部收割后才退出。测试侧同步修复：集成测试对子进程树的清理必须「terminate → wait 超时 → kill」兜底（吞掉 TimeoutError 不 kill = 泄漏），POSIX 收敛 supervisor 用 SIGINT 触发其自身收树逻辑。
- **research_run 特征计算多进程接通 + 加载期分块并行（#288）**：#287 去重后剩余瓶颈为单核计算，两层并行化，零语义变化（contexts 顺序 / 产物 checksum / 确定性重放 result_checksum / 具名 warning / #263 逐期失败标记 / #188 进度计数不变，均有等值单测锁定）。（1）**research 特征路径接通 process_workers**：settings 新增 `research_price_feature_process_workers`（默认 4，0 = 进程内协程池旧行为，仅 multi_period 逐期价格特征生效；single_shot / user_code 路径不启用）；复用 factor_lab 既有 spawn 先例（`_create_price_feature_process_executor`）并扩展 `max_tasks_per_child`（常量 128：worker 执行 N 任务后重启，摊销 Windows spawn 开销 + 防内存累积；fork 不支持该参数，本项目只在 spawn 用池）；新增 `PriceFeatureProcessPool`（async 上下文管理器）把池生命周期提升到「一次加载期」——`start()` 建池 + 预热一次，期内全部期次经 `build_price_feature_snapshot(process_executor=...)` 复用（外部池不建不预热不关闭，单次调用自建自毁的既有行为不变），加载结束 `aclose()`；`build_decision_load_contexts` 新参 `process_workers`，worker 侧 `build_signal_engine_adapter_factory` 经 settings_factory 解析（工厂缺失 / 抛错 / 非法值一律按 0 处理——性能开关不阻断 run）。（2）**加载期分块并行**：`build_decision_load_contexts` 逐期串行循环改按 `_DECISION_LOAD_CHUNK`（常量 4：常驻池 4 worker × 每期内部 8 并发读 ≈ 32 在途，由线程池上限自然钳制，不与 #287 的 gather+信号量无界叠加）分块 `gather`——纯读、结果按原始期序归位；日历与 close 矩阵（#287 惰性缓存）在分块前显式预建（`ensure_close_histories`，幂等；并发首建会打穿矩阵收益），并发首建收敛到单一顺序点；#263 失败标记：分块内 `return_exceptions=True` 收集后按原始期序重抛**第一个**异常（与串行首个失败一致），attach 标记后类型 / 消息 / traceback 不变。**进程池异常不炸 run**：池启动失败记 `research_run.process_pool_start_failed` 具名 warning 后本次加载全部期次降级进程内协程路径（结果逐值一致）；池中途损坏（BrokenProcessPool）记 `research_run.process_pool_degraded` 并 mark_broken，本期重算一次 + 后续期直接走进程内；worker 内业务异常（数据不足等）不是池异常，原样抛。等值锁定：`tests/unit/research_run/test_issue_288_load_multiproc.py`（真实 spawn 多进程等值 / broken pool 降级 / 分块 vs 串行 / #263 分块定位 / close 矩阵预建幂等，本机 Windows 无 fork）。纯研究/回测计算域，不连 broker 不下单，回滚 = settings 置 0 或 revert。
- **interrupted run 恢复通道（#305）**：RR-7a74 事故恢复死锁修复——run 被标 interrupted（checkpoint 保留）后 `run_replay` 仅接受 completed，只能 cancel 后手工带全部快照 ID 重新入队，而「进程重启」error_summary 却承诺了不存在的恢复入口。三处守卫同改（MCP `finboard_run_replay` / REST `POST /api/research/runs/{id}/replay` / `Coordinator.replay`）：**权威判定收敛为 `REPLAYABLE_SOURCE_STATUSES` = {completed, interrupted}**（`finboard_backtest.research_run.contracts` 导出，三层复用同一判定与同一 `replay_guard_error` 文案，错误区分 not_found / conflict 并附恢复路径）；interrupted 重放即恢复——新 run 经 `replace(source.manifest)` **自动继承全部冻结输入**（dataset_release_ids / factor_snapshots 全部 ID 零手工），血缘标注 `replay_of_run_id` + manifest 新字段 `replay_source_status`（completed=确定性重放对照 / interrupted=事故恢复；不入 `input_checksum`，None 时弹出 checksum 保持历史 manifest checksum 不漂移，同 strategy_version 先例）；interrupted 源无 result_checksum，重放即全新执行不判确定性漂移，completed 源行为零变化；cancelled 是显式用户意图仍拒绝；process_restart 的 error_summary 改为指向 `finboard_run_replay` 恢复通道。自动重排不做（job 侧 `requeue_due` 自动重试覆盖僵尸场景）。测试 `tests/unit/research_run/test_issue_305_replay_interrupted.py`、`tests/unit/test_issue_305_replay_channels.py`、`tests/integration/test_research_run_persistence.py`。纯研究域，不连 broker 不下单，回滚 = revert（历史 run 的 replay_source_status 为 null 自然兼容）。
- **close 矩阵预建按 explicit_symbols 声明域收窄（#299）**：#287 的 close 矩阵预建恒为全发布 instruments（`_build_candidates_and_lots(provider.release.instruments)`）——显式标的 run（#254 声明域）被迫为整只发布付一次性全量读取成本（真实数据实测：150 只声明 × 5534 只发布，加载段约 870s 全耗在预建）。`FrozenInputLoader` 新增 `_declared_domain_instruments`（与静态预检共用 `explicit_symbol_domain`，声明但发布中缺失的标的仍由 `_apply_universe_filter` 发具名 warning 不重复告警），`ensure_close_histories` 预建与 `load_context` 逐期机械字段加载（候选池 / decision/execution close / lot_info / 研究发布观测）**两处一起收窄**到声明域——只收窄预建会让逐期加载对域外标的全部走逐期回退读取（fallback 爆炸），必须同点收窄；未声明 explicit_symbols 时行为不变（全发布预建）。声明域内逐值等值语义不变（`_reference_close` 逐期 PIT 对照 + 逐期加载零回退读取断言，`tests/unit/research_run/test_issue_299_close_matrix_domain.py`，读取次数经 #285 `collect_parquet_read_stats` 断言 = 域内标的数）；研究发布 missing 清单（#252）随之只覆盖声明域，与 #254「评估域收窄、域外不产噪音」一致。纯研究域读路径优化，不触交易安全红线，回滚 = revert。
- **MCP 回测异步化（#189）**：`finboard_backtest_run`（strategy 形态）支持 `run_async=true` 显式入队 `kind=backtest_run` 后台任务返回 job_id（与 REST `POST /api/backtest/run` 同 `data` 队列、同 payload `{request, provider_name}`、同幂等键 `backtest:{strategy}:{digest}`）；省略 `run_async` 时按估算工作量（标的不数 × 交易日）自动切换异步，≥ 阈值 `backtest_auto_async_symbol_days`（settings，默认 15000，0=关闭）即异步，小规模默认同步行为不变（兼容既有调用方）。异步任务由独立 worker 消费，`finboard_job_get` 轮询（成功后 `result_ref=str(run_id)`），`finboard_backtest_history_get` 查完整结果；大任务不再 MCP 30 秒客户端超时后响应丢失、只能事后捞历史。纯离线研究域，不连 broker 不下单。
- **MCP 回测/入队工具易用性（#190）**：`selection.factor_version` 校验报错列出合法枚举 `[v1]`（双命名空间长篇澄清文案已随 #214 目录收敛精简，`framework_version` 仅一句带过）；`finboard_backtest_grid_get` 默认 `equity_mode=none` 不返回 equity 曲线（只留 `equity_point_count` 点数提示，响应最轻），显式 `summary`/`full` 才返回（此前 9 组合 × 200 点约 100-200KB 易被 MCP 客户端截断）；`finboard_run_queue` 描述附完整 payload 模板与各必填字段取值来源，澄清 `code_version` 为 **run 自身冻结字段**（冻结进 manifest/checksum 供追溯，与数据集发布的 `code_version` 同名但互不校验）。仅改 MCP 工具描述/默认返回体与文档，不触及回测语义与数据落库。纯离线研究域，不连 broker 不下单。
- **multi_period 快照依赖文档与报错对齐（#203）**：澄清 #183 措辞——multi_period 必须显式声明 `parameters.rebalance_frequency`，「不要求预建快照」仅限决策日推导与价格因子，基本面因子仍 PIT 取自冻结快照/研究数据发布。`signal_engine.build_decision_inputs` 报错按根因细分：声明多期但发布日历推导不出决策时点（每期期末须有下一成交日）与未声明频率的 single_shot 缺冻结快照分别报错，均附 `execution_mode` 与修复路径。入队期校验（REST+MCP 共用 `single_shot_snapshot_gate_error` 门控，对齐 #186 秒级失败风格）：single_shot 未冻结任何因子快照即 `invalid_argument`（依赖因子输入时附缺失因子源清单；multi_factor 无因子源同样拦截，因决策时点只能来自快照），multi_period 声明频率后不受影响；非法频率值由 `ResearchRunQueueIn` schema 入队即拒。纯离线研究域，不连 broker 不下单。
- **multi_period 因子可用性元数据（#253）**：把「identity 节点缺少数据源」从执行期前移到入队期——研究数据发布按 kind 可派生的特征集合成为**发布侧元数据**：canonical 映射 `RESEARCH_RELEASE_FEATURE_NAMES`（daily_metrics→{pb,turnover_rate,market_cap,earnings_yield,dividend_yield}，financial_indicators→{roe,gross_profit_margin,debt_to_assets,revenue_yoy}）移至 `finboard_data.releases` 唯一事实来源，发布构造器按 kind 冻结 `derived_features` 进 release manifest（空值不序列化，旧 manifest checksum 不漂移；universe_precheck 的 str-keyed 同名映射改为投影，与 `extract_factor_matrix` 输出的一致性由单测锁定）。入队期门控（REST `queue_research_run` 与 MCP `_build_queued_manifest` 共用 `multi_period_feature_gate_error`，#186/#203 风格，置于用户因子门控之后——multi_period 引用 u_ 因子先按 #217 具名拒绝）：multi_period 规格 identity 源必须 ⊆ 多期可解析集合（标准价格特征 / `close` / attached 研究发布 `derived_feature_names`（旧发布按 kind 回退）/ 快照观测），否则秒级 `invalid_argument` 具名缺失特征与所需发布 kind（无任何 kind 提供的特征明说并给修复路径），不再让 run 排队 + worker 开跑后才失败；single_shot 不受影响（由 #203 门控把关）。`strategy_validate` / `run_queue` 的静态预检（`resolvable_feature_names(research_release_kinds=...)`，#213 路径）与门控消费同一映射，不重复实现。AGENTS/Skill/MCP `_INSTRUCTIONS` 与 `finboard_run_queue` 工具描述同步。纯研究域校验/元数据层，不改回测语义与落库 schema（`derived_features` 由 manifest JSON 自然携带），不触交易安全红线，不连 broker 不下单。
- **研究/回测 MCP 返回瘦身（#206）**：按 P0→P3 逐级把默认响应从 MB 级降到 KB 级，全部为纯展示层变换，不改语义、不动落库（与 #172 equity 降采样同先例）。P0 详情接口 `view=summary|detail` 默认 summary——`dataset_release_get` 省略逐标的 instruments 数组（复用 `_release_summary_to_dict`，detail 走 `as_dict()` 诊断）；`run_get`/`report_run` 把 universe 逐标的判定服务端聚合为 `{total, included, excluded_by_reason}`、fills 按决策计数、metrics 剔 equity_curve，不序列化 manifest/result/逐标的全量 payload；fills 分页默认有界 200 条（`backtest_history_get`/`report_backtest`，`fills_limit=null` 全量，`fills_total` 元数据完整，REST/MCP 导出文件保持全量）。P1 写操作回显精简——`run_queue`/`strategy_draft_create`/`strategy_publish` 返回 `{id, version, status, checksum, execution_mode, created_at}` 精简回执（附 detail_hint），全文走对应 get，`backtest_run(strategy_spec)` 路由同步适配；`grid_get` 网格级公共字段（strategy/symbols/start/end/capital/adjust/params=base_params）在头部只出现一次，combo 去掉 params（组合差异由 label 承载）。P2——`backtest_history_list` symbols 前 10 只 + `symbol_count`；`job_get` 加 `view=none|summary|detail`（none=轮询最小集，summary 默认剥 payload）；信封 `ToolEnvelope` 挂 pydantic 自定义序列化，structured content 省略恒为 null 的可选字段。P3 幂等短路——`job_get` 返回附 `data_hash` 状态指纹，轮询回传未变即 `{unchanged: true, data_hash, status}` 不重发全量。回滚 = 各参数显式传 detail/全量即可。纯离线研究域，不连 broker 不下单。
- **绩效指标口径与 Sharpe rf 标注（#262）**：研究域全部 Sharpe 实现（引擎 metrics / research_run 组合管线 / mean_reversion / futures_tsmom / validation 统计）统一委托 `finboard_backtest.metrics._sharpe_core`，口径由参数显式承载（rf 按 `rf/252` 日化、ddof=0 总体 / ddof=1 样本标准差、√252 年化）。**引擎主口径 `sharpe_ratio` 保持 rf=3%/ddof=0 不变**（历史落库语义零漂移），报告随指标序列化 `risk_free_annual`（实际 rf 取值）与 `sharpe_rf0`（rf=0/ddof=1 对照口径）；research_run 报告 `sharpe_ratio` 口径不变（rf=0/ddof=1），新增 `risk_free_annual=0.0` 标注（实现改委托统一核心，等值重构）；mean_reversion（rf0/ddof1）、futures_tsmom（rf3%/ddof1）、validation（默认 rf3%/ddof0，PBO 排名场景显式 rf=0）全部等值重构、行为不变。**跨报告同屏比较规则：引擎报告取 `sharpe_rf0`、研究报告取 `sharpe_ratio`（两字段同口径 rf=0/ddof=1）**——勿拿两边的 `sharpe_ratio` 直比（rf 与 ddof 双重口径差；低收益策略主口径会被 rf=3% 拖近 0，如 run 277 年化 3.05% 显示 Sharpe 0.05 的误读）。REST `BacktestMetricsOut` 与 MCP `backtest_run`/`backtest_history_get`/`report_backtest`/`grid_get` 序列化与工具描述同步，grid 指标矩阵新增 `sharpe_rf0` 列（旧 run 无该键时列自动省略，`risk_free_annual` 键在逐组合 metrics 可见不单列）；报告文本 `summary()` 标注口径。五套实现口径映射有专项回归锁定（`tests/unit/backtest/test_issue_262_sharpe_conventions.py`）。纯指标/展示层，不改交易逻辑与落库 schema（metrics 为 JSON 列自然携带新键），不触交易安全红线，不连 broker 不下单。
- **后台任务归档（#221）**：`background_jobs` 新增 `archived_at` 可空列（独立于 status 的展示维度，不新增状态枚举值、不改状态机）——归档后从默认列表（`GET /api/jobs`/`finboard_job_list` 的 `archived=exclude`）隐藏但**不删除**，`archived=only|all` 与单查（`GET /api/jobs/{id}`/`finboard_job_get`）始终可达，可取消归档（幂等）。**仅终态（succeeded/failed/cancelled/interrupted）任务可归档**，非终态归档请求 fail-closed 拒绝（排队/运行中任务被藏起来没人看见是事故温床）；归档即冻结——worker 维护路径 `requeue_due` 跳过已归档行，不会自动重排回队列。批量归档按 kinds/statuses（终态子集校验）/queues/finished_before 过滤、created_at 从旧到新、limit 夹紧 1..1000，REST/MCP 批量只回 `archived_count` 计数不回全量（#206 精神）。MCP 新增 `finboard_job_archive`/`finboard_job_unarchive`（写，受 `mcp_readonly_only` 门控），`finboard_job_list` 加 `archived` 参数；前端任务中心支持归档过滤器（默认排除）、终态行「归档」/归档行「恢复」、批量「归档已完成」（只收 succeeded/cancelled，failed/interrupted 留列表便于排查）。回滚 = 迁移 downgrade 删列 + 移除 2 个新工具与 archived 参数。纯研究/数据域调度表展示维度，不连 broker 不下单。
- **universe 市值过滤与 ST 真实判定（#213，blocked by #212 数据就位）**：`UniverseSpec` 新增 `min_market_cap` / `max_market_cap`（人民币元，与 tushare `total_mv`×1e4 落库的 `daily_metrics.total_market_cap` 同单位），运行时取冻结因子快照 / 研究数据发布联合链路（#187）的 `market_cap` 特征观测，缺失按 `missing_market_cap` 排除并入空池根因字段（`market_cap_below_minimum` / `market_cap_above_maximum` 与 price 同风格）。`exclude_st` 不再恒不生效——signal_engine 移除 `is_st=False` 硬编码，按发布 instruments 的 `name_history` 半开区间取**决策日名称**做 PIT 判定（与 v1 selection 同谓词「名称含 ST」，覆盖 ST/\*ST/S\*ST）；无覆盖区间回退当前名称近似、缺名称按非 ST 处理，两条降级路径分别发 `universe_st_pit_approximate` / `universe_st_filter_inactive` 具名 warning 而非静默放行（静态预检与运行时每决策共用 `universe_filter_warnings`）。universe 预检（REST+MCP）同步：`universe_market_cap_unavailable` 具名 warning（market_cap 在可解析特征集合时不排除、不误报空池）；`resolvable_feature_names` 新增 `research_release_kinds` 入参，附 daily_metrics / financial_indicators 研究发布时其派生特征名（market_cap/pb/turnover_rate/roe 等）计入静态可解析集合，防 multi_period（无因子快照）误报空池。纯离线研究域，不连 broker 不下单。
- **因子目录收敛（#214，用户确认：v2 唯一事实来源 + v1 投影层、历史不迁移）**：`finboard_data.factors.FACTOR_CATALOG`（v1 selection 目录,8 因子）不再独立维护——逐字段从 `FACTOR_LAB_CATALOG`（26 因子）投影生成（`_project_v1_catalog()`）：dependencies=factor_lab `source_fields`、frequency 映射 DAILY/REPORT、description=`economic_hypothesis`，仅原始值单位（cny/multiple/ratio,v2 登记的是标准化 z_score 单位）保留为 v1 展示映射；v1 名称在 v2 目录缺失时导入期即 RuntimeError，杜绝漂移。`selection.factor_version=v1` 行为与可用因子集完全不变（历史 run 不迁移、无破坏）；`finboard_factor_catalog` 展示的 v2 目录是唯一事实来源，v1 selection 可用因子集 = `FACTOR_CATALOG` 键 = 其子集（Skill `references/tools.md` 载映射说明）。#190 的 factor_version/framework_version 双命名空间长篇澄清文案随之精简。纯展示/校验层重构，不改回测语义与数据落库，不连 broker 不下单。
- **复合评分因子目录语义字段投影（#226，继 #214 因子目录收敛）**：`finboard_backtest.factors.catalog.RESEARCH_FACTOR_CATALOG`（复合评分元数据,13 因子）不再手写经济假设/预期失效/数据来源/方向四个语义字段——由 `_project_research_catalog()` 从 `FACTOR_LAB_CATALOG` 逐项投影（direction=preference 映射 higher→LONG/lower→SHORT,EXPOSURE_ONLY 无方向映射导入期 RuntimeError;因子名在 v2 缺失同样 fail-loud）;评分消费参数（category/winsorize pcts/standardize/missing_strategy/available_at_rule/原始值 unit）保留本地 `_SCORING_PARAMS`,不塞进 v2 目录。13 因子 `direction_sign` 与 compiler `_dataset_from_source` dataset 映射冻结断言逐项不变。纯展示/元数据层重构,不改评分与回测语义,不连 broker 不下单。
- **正式研究组合入口（#91）**：long-only `ResearchRun` 必须由 `PortfolioPipelineAdapter` 从冻结候选池/特征/信号生成目标、硬约束、风险退出、三档资金可行性、研究订单/成交和账本；风险贡献上限启用后缺少协方差、数学不可行或不收敛必须在生成研究订单前失败关闭。
- **组合层风险因子中性化 + 最大 IR 加权（#266）**：`PortfolioConstraints.risk_factor_limits`（`[{factor, max_active_exposure}]`，经 `portfolio_config.overrides.risk_factor_limits` 声明，因子名与冻结特征 `feature_id` 同名，行业 one-hot 展开为逐行业列）对 active 暴露 `|Σ(w−baseline)·f| ≤ 阈值` 做**只减仓投影**（同号 offender 按暴露线性闭式解一步缩放、释放权重转现金、多因子交替迭代，位于全部其他约束阶段之后且不破坏单资产/sleeve/gross/现金上限；与风险贡献上限潜在冲突时复核失败关闭）——`project_risk_factor_neutralization` 逐因子审计进 builder 约束行；暴露观测（`PortfolioBuildInput.factor_exposures`，管线从冻结特征按声明因子名提取）缺失的因子降级为具名 warning（`factor_neutralization_inactive:<factor>`，skipped 软约束行 passed=False 可见不静默），约束本体不可满足（如基准权重主导暴露、减权无进展）才 fail-closed。active 度量域为持仓 ∪ 基准并集（`neutralization_baseline` 缺省现金基准=零向量）。`AllocationMethod` 新增 `max_ir`：最大 IR（切点）组合分配器，LW 协方差子阵 + `score×confidence` 信号强度代理预期超额收益，capped simplex 投影梯度 + Armijo 回溯（纯 numpy，**零新增依赖**；伪凸目标一阶平稳点即全局最优；确定性可复现）；求解失败与 ERC 同语义——默认 fail_closed 报错，`covariance_failure_mode=fallback_equal_weight` 时降级等权并打 fallback 标记。策略规格向后兼容：`max_ir` 仅可选声明（overrides 字符串直通 + 规格枚举映射），旧规格 checksum/行为零漂移。REST `POST /api/portfolio/allocate` 与 MCP `finboard_portfolio_allocate` 同步暴露 method=max_ir 与中性化三参数。纯离线研究域，不连 broker 不下单。
- **产品模拟盘边界（#83）**：`finboard-simulation` 只接受已发布策略和 completed `ResearchRun` 的结构化目标仓位，订单、成交、持仓、资金、时钟与审计全部持久化在独立模拟表。禁止导入/配置 Broker 或 QMT/CTP、禁止直接创建订单或修改持仓、禁止自动晋级影子盘/实盘。
- **研究策略配置边界（web 通道）**：允许用版本化无代码规格组合后端白名单候选池 / 因子 / 信号 / 目标仓位 / 研究风控 / 成交假设 / 验证计划；禁止网页提交 Python、模块路径或可执行表达式，保存 / 发布配置不得自动启动任何运行。
- **研究代码提交边界（MCP agent 通道，#215）**：外置研究 agent 可经 `finboard_research_code_submit` 受控提交策略/因子 Python 代码到本地 bare git 仓库（settings `research_code_repo_path`，git 写操作收敛在服务端，agent 容器文件系统只读）。提交前静态校验：manifest 必填、入口签名（factor.compute / strategy.decide）、import 白名单（pandas/numpy/polars/math/statistics/finboard_research_kit）、禁 subprocess/socket/文件写模式/eval/exec、文件数与单文件上限、拒二进制。每次提交登记 `research_code_artifacts` 为 draft/pending（同名重提交不替换当前通过版本、晋级后旧版 retired、可 rollback / diff）。只存储与版本化；执行只经 #216 沙箱通道；LLM 产出仍须走 研究→回测→OOS→模拟→影子→小资金 完整晋级链。web 通道仍禁代码（两条通道边界不变）。
- **研究代码沙箱执行边界（#216，单形态 Docker 一次性容器）**：通过晋级门的 active 因子代码经 `finboard_research_code_run`（`kind=research_code_run` 后台任务，worker 单并发）在一次性容器内执行协议 v1 纯截面函数 `factor.compute(ctx) -> FactorResult`（ctx/harness 由新包 `finboard-research-kit` 提供，镜像 `docker/research-sandbox/`，tag 与 kit 版本绑定，CI 构建，run 记录镜像 digest）。容器加固：`--network none` / `--read-only` 根 / `--cap-drop ALL` / 非 root / `--pids-limit` / CPU 与内存限额（memory-swap 同值禁 swap）/ 墙钟超时 `docker kill`；数据面为按 `decision_at` 物化的只读挂载——**PIT 由物理隔离保证**（挂载内容即决策时点之前的数据，容器内不存在未来数据文件；provider 门控之上再设「数据日期晚于 decision_at 当日即拒绝生成挂载」的 fail-closed 防线），附 `mount_manifest.json` 清单。run 记录（`research_code_runs`，`RCR-` 前缀）持有 code commit × 数据 release × 输出 scores checksum 三向引用；stdout/stderr/退出码/超时/OOM/资源用量归档 workspace。失败分类：`static_validation_failed`（执行端重放 #215 校验）/ `runtime_error` / `timeout` / `oom_killed` / `output_contract_violation` / `sandbox_unavailable`。默认关闭（`research_sandbox_enabled=false`，需 Docker Desktop；沙箱无常驻服务，镜像由 `finboard dev` 预检在开关开启且缺失时自动构建，与 CI 同源 Dockerfile），回滚 = 关开关 + 迁移 downgrade + 移除 2 个工具；容器级 E2E 默认跳过（`FINBOARD_SANDBOX_E2E=1` 开启）。纯离线研究域，容器无网络无凭证，不触实盘任何组件。
- **沙箱因子接入选股管线边界（#217）**：沙箱执行成功只是第一步,输出必须过**质量门**（NaN 比例 ≤ `research_sandbox_max_nan_ratio`、覆盖率 ≥ `research_sandbox_min_coverage`,默认各 0.5;不合格 run failed=`quality_gate_failed`,错误指明阈值,结果含阈值记入 `metrics.quality_gate`）才落库为 `factor_feature_snapshots` 兼容观测——`dataset_release_id` 可空 + `source_run_id` 锚定产出 run（`RCR-`）,`dataset_release_checksum` 承载挂载 manifest checksum,`research_code_runs.output_snapshot_id` 回填引用;观测因子名 = `u_<artifact_name>`（`u_` 前缀与内置目录隔离,防重名）。可引用性三道闸:**编译期** `compile_strategy_spec(user_factor_sources=active+passed 名单)`（REST/MCP validate、publish 共用,名单外即 StrategySpecError）;**入队期** `user_factor_reference_gate_error`（REST+MCP 共用——retired/不存在/未通过晋级秒级拒绝;multi_period 声明 rebalance_frequency 引用用户因子秒级拒绝,因观测绑定单一 decision_at;沙箱快照按锚定 run 的发布集合校验 ⊆ 冻结清单）;**执行期** 数据全部来自冻结 manifest（快照 checksum 已冻结,artifact 后续 retired 不影响已入队运行）。`finboard_factor_catalog` 展示混合目录（builtin 26 + user_defined 标注 commit/status/promotion_status,仅 active+passed 可引用）。引用用户因子的 research run 在 report 携带 `factor_screen` 段:rank_ic / rank_ic_ir（spearman 序列,≥2 期才有 IR）/ 分层收益（5 桶等权 forward return）/ 换手率 / 与既有因子相关性矩阵;forward 窗口 = 相邻决策时点 PIT close（末期为发布区间末）;计算尽力而为,失败只记 warning 不阻塞 run。回滚 = 迁移 downgrade + 质量门阈值调 0;无新工具名,工具总数 124。纯离线研究域,不连 broker 不下单。
- **用户代码策略执行边界（#218，逐日决策函数）**：L3 沙箱路线第四环——agent 编写的**策略**代码进入回测。`strategy_kind=user_code` 的 strategy_spec 携带 `code_artifact {name, commit?}`（只引用 kind=strategy 的 active+passed artifact，不携带代码；web 无代码边界不变；feature_graph/signal_rules 允许为空）。research_run（建议 `parameters.rebalance_frequency=monthly|quarterly` 走 multi_period；single_shot 需冻结快照提供决策时点）逐决策日在一次性容器（`--mode strategy`，挂载清单 v2 含当前权重回显与约束视图）执行 `strategy.decide(ctx) -> targets`（kit v0.2.0 `StrategyContext`：PIT 数据视图 + 当前权重回显与 `StrategyConstraints` 只读视图）；目标权重映射为信号后**复用 #91 组合管线全部机械**（`direct_weights` 分配路径不重缩放，负权重/超上限由约束投影逐项截断审计；池外/缺执行元数据标的丢弃记 warning）——**策略只出目标权重，不触任何订单语义**。**候选池语义（#254）**：`PortfolioDecisionInput.candidates` = universe 过滤后的 included 集（与 multi_factor 引擎同一口径，不再透传发布全 ready 标的）；decide 输出的池外/缺执行元数据标的仍由 `targets_to_signals` 丢弃 + 具名 warning 兜底（#218 原语义不变）。入队门控（REST+MCP 共享 `user_code_reference_gate_error`）：artifact 非 active+passed / commit 与 active 不一致 / 沙箱未启用 / single_shot 缺快照 → 秒级拒绝；放行时 active commit 冻结进 manifest（input_checksum 覆盖代码版本）。report 附 `sandbox_provenance`（code commit + 镜像 digest + 逐决策 targets checksum），与 multi_factor 同屏可比。v1 不做事件驱动 on_bar（日内形态另行开 issue）；镜像 tag 升 `finboard-research-sandbox:0.2.0`（kit 版本三处同步）。回滚 = kit 0.1.0 + 移除 registry/dispatch/gate；无新工具名。纯离线研究域,不连 broker 不下单。
- **研究代码验证门与晋级链路（#219）**：MCP `finboard_research_code_submit` 只登记 `draft/promotion_status=pending`，不会把未验证代码放入正式组合或模拟盘白名单；`finboard_research_code_promote` 必须同时绑定同一 artifact/name/kind/commit 的 screen 运行与 #57 `validated_oos + final_test_unsealed=true` 实验，并通过 IC、换手率、相关性上限后才转为 `active/passed`。screen 门的 rank_ic 取**绝对值是有意设计（#245 用户决策 A，2026-08-31）**：门只证「存在非噪声信号」，方向正确性由因子目录 preference 与 screen 权益/换窗复测承担，负 IC 方向型因子过门不构成缺陷（行为由单测锁定）。失败证据保留在 draft，旧版本回滚也只重新建立 draft，retired/未晋级引用必须 fail-visible。
  ```text
  submit -> draft(pending) -- screen + #57 OOS --> active(passed)
                         \-- gate failed --------> draft(failed)
  active(passed) -- 新版本晋级或显式退役 --> retired
  rollback(old commit) -----------------------> draft(pending)
  active(passed) -> 正式 composite / simulation whitelist
  ```
- **验证实验执行任务化（#233）**：`finboard_validation_experiment_run`（写，`mcp_readonly_only` 门控）把「按计划跑 walk-forward + 一次性揭盲」登记为 `kind=validation_experiment` 后台任务（worker 单并发，`max_attempts=1`），补齐 #219 晋级门 OOS 半边的运营入口（此前 `ValidationRunner` 只有单测调用，实验永远到不了 `validated_oos`）。入队预检秒级拒绝不可运行/预算耗尽/已揭盲（执行端重放同组检查双保险）；`ValidationExperimentExecutor` 逐 trial 落库（失败也算试验、逐 trial 提交无半写）→ walk-forward（OOS 门+稳健性+DSR/PSR/PBO）→ 一次性揭盲；**揭盲不可重做**（终态后重复执行抛具名 `experiment_not_runnable`，不产生第二次最终测试集评估）；断点续跑按确定性 trial_id upsert、`trials_used` 不重复递增。runner 工厂按实验 `selection_config.validation_trial_runner {strategy, symbols, provider?, params?, capital?}` 构建注册表策略回测，未声明报具名 `trial_runner_unconfigured`；测试注入合成 runner。**执行健壮性（#244，第二轮 E2E 实测阻断修复）**：PBO/CSCV 矩阵取 IS 阶段各竞争 trial 的日收益（同窗同轴天然等长，符合「竞争配置」语义），不再用顺序 walk-forward 窗口序列（近似交易日切窗 vs 真实交易日产出点数不等 → 等长断言必崩）；可比 trial <2 / 非矩形 / 续跑无内存收益 → PBO 具名跳过写 `methodology_notes`；`capital`（int/float/数字字符串→Decimal）/`params`（mapping）/策略名与基础参数在 runner 构建期具名 `trial_runner_invalid_config` 预校验，不消耗试验预算；walk-forward/揭盲段意外异常兜底为具名 `experiment_execution_failed`（附持久化状态与续跑指引），实验状态不被推进、可修复后重入队断点续跑。回滚 = 移除 executor 注册与该工具，实验仍可走手工 runner 路径。
- **screen 用途 draft 产物显式绑定通道（#234）**：解除「draft 产物拿不到 screen 证据 → 永远无法第一次 promote」死锁（factor/strategy 双通道）。规格新增 `screen_artifact_bindings`（`{kind, name, artifact_id, commit?}`；`code_artifact` 增可选 `artifact_id`）：**声明层**编译期把绑定名并入本规格可引用名单（仅声明绑定的规格生效，普通引用门行为不变）；**实绑层**入队期（REST+MCP 共用 `resolve_screen_bindings` / `screen_factor_snapshot_gate_error`，#186/#203 秒级失败风格）按 DB 逐条校验存在/非 retired/kind·name·commit 一致，factor 通道还要求绑定产物的 RCR 快照已进 `factor_snapshot_ids`；**冻结层** strategy 绑定 commit+artifact_id 冻结进 manifest（factor 由快照 `source_run_id → RCR` 追溯承载）；**兜底层** promote 四向一致性校验不变（screen 运行不可挪作他版代码的证据）。顺带修复 #218 遗留缺陷：规格未声明 commit 时冻结改变 payload 后 manifest checksum 未重算导致入队必败。无新工具名（工具总数 125）。回滚 = 移除绑定字段与门控分支。
  每次晋级与沙箱运行归档 code commit、dataset release/checksum、参数/checksum、output checksum 四向引用以及容器日志/资源；模拟盘仍只消费已发布策略和 completed `ResearchRun` 的结构化目标，订单/成交/持仓/资金/审计只写 `simulation_*` 表，不导入 broker，也不自动晋级影子盘或实盘。该治理链路不改变实盘下单、持仓恢复或风控逻辑。
- **AI 能力边界（#84 建立，#160 全面迁移 OpenCode）**：FinBoard 自身**零内置 LLM 调用**——#84 的 `ResearchAssistant`/`OpenAICompatibleLLMProvider`/`finboard.ai.*` MCP 工具/`/api/research/ai` REST/前端审批中心与 `factor_hypotheses`/`factor_hypothesis_experiments`/`ai_drafts`/`ai_audit_events` 四张表已随 #160 移除。AI 智能由 OpenCode 研究运行时的外层 LLM 承担，FinBoard 只提供研究/数据域 MCP 工具；#57 机器验证本体（`research_experiments`，REST `/api/research/experiments` + `finboard.validation_experiment.*` MCP 工具）与因子实验（`finboard.factor.experiment.*` 的 `validation_experiment_id` 引用）保留，OOS 机器验证闭环不受影响。LLM key 配置迁移到 OpenCode 侧（`opencode_env_overrides` 注入）。
- **实盘恢复条件**：QMT 权限解决 → #22 真机验证 → #23 标的元数据 → #24 连续运行 → 小资金实盘。
- **P6 及以后禁止提前开工**：TWAP / VWAP / 冰山单 / 智能拆单、跨账户路由、复杂回测、因子系统、LLM 接入。

### 交易安全红线（改动这些逻辑必须先与用户确认风险）
- `client_order_id` 必须由本地生成且全局唯一，用于防重复下单 / 关联券商订单 / 状态恢复 / 排查异常
- **下单请求超时后禁止无条件重试**（这是文档明确指出的最危险场景）：订单须先进入 `UNKNOWN` 状态，查券商委托记录确认原订单不存在后才能重发；查询类请求可重试。重启恢复（`RecoveryEngine`）在 `kernel.start()` 中自动执行：逐订单查券商 → 修复状态 → UNKNOWN + 券商无记录 → REJECTED（不自动重发）
- 订单必须走完整链路：`Strategy → Order Intent → Risk Manager → Order Manager → Broker Adapter → Broker API`。**策略不得直接调用券商接口**
- 持仓的最终真实来源是**券商查询**；本地持仓仅用于实时响应 / 策略计算 / 风控 / 异常检测。**禁止策略直接修改持仓数量**，持仓只能由成交 / 公司行为 / 交割 / 人工校准等事件驱动
- 系统重启后必须先完成本地 ↔ 券商核对，核对通过前**禁止发送新订单**（恢复流程见 `phase1_doc.md` §5.4）
- Kill Switch（暂停新单 / 仅允许减仓 / 撤全部活动订单 / 暂停某策略某账户 / 全局停止）必须由**交易内核**执行，不能只依赖网页按钮
- 实盘资金/仓位预占与并发失败关闭缺口已登记为 #92；在用户确认并恢复 QMT 真机验证前不得以离线 #91 风控替代或提前实施

### LLM / Agent 边界
LLM（包括本 agent 自身）**不允许**：直接连接实盘账户、直接发送订单、修改账户持仓、绕过风控、在实盘运行时动态生成代码并立即执行。LLM 的产出必须经过完整流程才可上实盘：`研究 → 回测 → 样本外 → 行情回放 → 模拟交易 → 影子交易 → 小资金实盘 → 扩大资金`。

**#160 起 FinBoard 不再内置任何 LLM 调用**：`ResearchAssistant`/`OpenAICompatibleLLMProvider`/`llm_*` 配置与 `finboard.ai.*` MCP 工具已整体移除，AI 问答、假设与策略草案由 OpenCode 运行时的外层 LLM 直接承担。FinBoard 侧的安全控制收敛为：MCP 工具入参统一 `sanitize_prompt` 脱敏 + 审计（`mcp_audit_events`），研究写操作由 agent 自主执行（#122），实盘能力永久不注册为工具。`factor_research` 包仅保留 `sanitizer.py`。

- **FinBoard MCP Server 边界（#108，OpenCode 外置研究 Agent；#122 放开写操作审批门）**：`finboard-mcp` 包把研究能力以受控 MCP 工具形式暴露给外置 Agent 运行时（OpenCode），FinDashboard 仍是唯一事实来源。MCP 工具复用现有 service/repository，不直接连接数据库做裸 SQL（#160 后不含任何 LLM 工具）。权限矩阵：**研究写操作（创建因子/快照/策略/运行回测/发布数据/启动模拟盘）agent 可通过 MCP 自主执行**（#122 放开，不触及交易安全红线：不连 broker/账户/订单/持仓）；**实盘能力（下单/撤单/改持仓/Kill Switch/连接 broker/凭证探测）仍永久不注册为工具**。每次工具调用统一记录审计事件（structlog 结构化日志 + 内存副本；`mcp_audit_persist=true` 时追加到独立 `mcp_audit_events` 表，REST `GET /api/mcp/audit` 可查询，#157 接线），不记录 API Key/原始凭证/未脱敏思考内容，入参经 `sanitize_prompt` 脱敏。工具返回统一信封 `ToolEnvelope`（operation_id/status/data/error/provenance/idempotency_key）。回滚方案：关闭 MCP 入口（不启动 server）即可，不影响现有 REST 入口与研究产物/审计。OpenCode 接入（#109）、Skill 与记忆（#110）、前端增强（#111）为后续 sub-issue。
- **OpenCode 研究运行时边界（#109，#121 重构后简化；#157 移除 basic auth）**：`finboard-opencode` 包把 OpenCode（`opencode serve` / Docker 化 `opencode web`）作为受控研究 Agent 运行时接入 FinDashboard。**OpenCode 自身管理会话 / 历史 / 恢复；FinBoard 不再维护独立 conversation 投影层**（#121 移除了 `ConversationService`、`agent_conversations`/`agent_events` 表与 `/api/agent/conversations` 路由；会话 DB 持久化在容器命名卷 `opencode-data` → `/root/.local/share/opencode`）。`finboard-researcher` agent 在 `.opencode/agent/` 定义，`permission` 为显式 allowlist（#157 建立，#182 扩展）：`"*": "deny"` 默认拒绝全部，放行 `read`/`glob`/`grep`（Skill 渐进式加载与项目文档只读）、`skill`、`bash`（**仅只读轮询/状态检查**：如 `wget`/`sleep` 探测 HTTP 端点与任务进度；容器沙箱无 FinBoard 凭证，且 `.opencode`/`.agents`/运行时配置挂载一律 `:ro`，agent 无法改写仓库配置）与 `finboard_*` / `exa_*` MCP 工具（`exa` 为官方托管 remote 端点 `https://mcp.exa.ai/mcp`，匿名限速可用，API key 可经 `x-api-key` header 或 URL `?exaApiKey=` 注入，需先经 `opencode_env_overrides` 传入容器）；`edit`/`write`/`webfetch` 仍拒绝。**`OpenCodeRuntimeClient` 的两种部署形态**：（A）web 容器模式（`opencode_web_enabled`，#118 Docker 隔离）—— runtime client 复用托管的 `opencode web` 容器（`base_url` = 容器端口如 4097，无 auth：#157 用户决策单用户模型移除 basic auth，宿主机侧 127.0.0.1 绑定是唯一网络边界），该容器同时服务 iframe UI 与 `/api/*`，**不再需要独立的 4096 `opencode serve`**。（B）外部 serve 模式（仅 `opencode_enabled`，向后兼容）—— 连接外部已启动的 `opencode serve`（默认 4096，无 auth）。两种形态都不连接实盘 broker/账户/订单/持仓/Kill Switch；`opencode_enabled`/`opencode_web_enabled` 默认关闭，回滚方案为关闭入口回到现有 REST 入口。
- **研究 Skill 与长期记忆边界（#110，#122 更新审批门语义）**：项目级 `.agents/skills/finboard-opencode-research/` 提供研究 Skill（`SKILL.md` + `references/` 渐进式加载，含工作流/工具选择/权限边界/来源引用规则）。**Skill 不授予任何工具权限**——权限由 OpenCode 配置（`.opencode/opencode.json` agent permission）与 FinBoard MCP 服务端策略共同控制，Skill 只描述「应该做什么」。研究长期记忆持久化在独立 `research_memories` 表（`source_refs` 关联数据集/策略/实验/ResearchRun/Simulation 产物，只引用不修改产物），不写入实盘 `orders`/`fills`/`positions`/`audit_logs`。记忆操作（`finboard.memory.*` 记住/忘记/纠正/确认/归档/列表/详情）只操作 `research_memories` 表、不创建 ResearchRun/回测/模拟盘、不触及实盘，因此自动允许；`created_by` 由工具标记（`agent:mcp`/`user:api`），不可伪造。**研究知识沉淀设施（#268，层 1 文档协议 + 层 2 只读挂载；层 3 memory_type 扩枚举暂不做）**：容器挂载白名单新增 `docs/research` → `/workspace/docs/research`（一律 `:ro`，宿主机目录不存在时优雅跳过、不阻塞容器启动）；`docs/research/` 三件套——`ROADMAP.md`（目标阶梯 8%→12%→15%/路线 A-D 状态/当前证据门）、`FINDINGS.md`（结论注册表：结论/置信度/证据 ID/失效条件/状态 有效-待验证-已证伪）、`rounds/YYYY-MM-DD-<slug>.md`（轮次日志）——以 PR 维护、为跨会话研究知识的 canonical 事实源（用户/主 coding agent/研究 agent 三方共享），记忆只存指针（保留 tags `research-plan`/`research-round`/`finding-confirmed`/`finding-refuted`，`finboard_memory_list` 按 tag 过滤），文档与记忆冲突以文档为准；Skill 增两协议——会话启动协议（读 ROADMAP/FINDINGS → `finboard_memory_list(status=active)` → 检索相关 run/experiment，挂载缺失跳过并注明）与轮次收尾协议（固定模板记忆 + 轮次摘要供转录进 rounds），并立重测禁令：未查结论索引禁止重测已证伪假设，重开须有新证据并显式引用旧结论。无新 MCP 工具（#123 的工具注册与 `_INSTRUCTIONS` 不涉及）。回滚方案：移除 Skill 目录与记忆表（迁移 downgrade），不影响研究产物/审计历史；#268 回滚 = revert 挂载条目与文档/Skill 变更（删除 `docs/research/` 目录即自然停用挂载）。
- **OpenCode Web 研究工作台网关边界（#118，已升级为 Docker 隔离；#121 重构后 access 不绑定 conversation；#157 移除 basic auth）**：`finboard-opencode` 的 `OpenCodeProcessManager` 托管一个**容器级隔离**的 `opencode web` Docker 容器（独立命名卷 `opencode-data`/`opencode-config` 持久化会话 DB 与 auth + 版本锁定镜像 `ghcr.io/anomalyco/opencode` + 环境变量严格白名单，**绝不**继承 FinBoard 的 DB 密码/broker 凭证/API Key + 宿主机侧 `127.0.0.1` 绑定）。**#157 用户决策：单一用户模型下 OpenCode Web 不启用 basic auth，直接使用明文 `http://127.0.0.1:{port}` URL；127.0.0.1 绑定是唯一网络边界，后续多用户需恢复网关鉴权层。**FinBoard 网关是 OpenCode Web 的**控制面**：`/api/opencode/status`（脱敏状态，含 `container_id` 与内嵌 finboard-mcp 运行状态）、`/api/opencode/health`（代理探测）、`/api/opencode/access`（网关启用即签发明文 `web_url`，无凭证字段），**不透传** OpenCode 流量（端口仅宿主机 loopback 可达）。因 OpenCode v1.18.15 不支持 `--base-path` 子路径部署（上游 PR #28326 审核中），前端采用 **iframe 跨源嵌入**而非子路径反代；待上游支持后可升级为同源子路径，上层契约不变。`opencode_web_enabled` 默认关闭，回滚方案为关闭入口回到现有 ResearchAssistant/REST 入口。OpenCode Web 工作台**不连接**实盘 broker/账户/订单/持仓/Kill Switch。**容器内 opencode 通过 `host.docker.internal:8765` 跨网络访问宿主机 `finboard_mcp`**（`finboard_mcp` 为 streamable-http + Bearer 鉴权；MCP 地址由 `opencode_mcp_remote_url` 在容器启动时渲染进 `.opencode/runtime/opencode.json` 单文件 bind mount，#157 接线；内嵌 MCP 在容器模式默认绑 `0.0.0.0`，token 保护），`-e` 只注入 LLM key / MCP token / HOME 与 XDG 目录语义，FinBoard 自身凭证不进容器。**启动顺序约束（#250）**：opencode v1.18.15 启动那一刻首连 remote MCP，失败即标 `failed` 且**不再重试**（整个容器生命周期 `finboard_*` 工具缺失），因此 lifespan 中内嵌 `finboard_mcp` 必须**先监听、容器后创建**——`_start_embedded_mcp_server` 后经 `_wait_embedded_mcp_ready` 轮询 8765 直到有任何 HTTP 响应（401 即算就绪，10s 超时只 WARNING 不阻断）才 `process_manager.start()`；容器就绪后再 GET 容器 `/mcp` 自检 finboard 状态，非 `connected` 打含 status/error 的 WARNING（`_check_opencode_mcp_status`），不再静默。**容器目录语义（#112 实测）**：注入 `HOME=/workspace`（文件选择器/homedir 从工作目录开始，否则「搜不到 workspace」）与 `XDG_*_HOME` 指向 volume 挂载点的父目录（opencode 会在 XDG 数据根下追加 `opencode/` 子目录，值必须是父目录才能让最终落点正好是 named volume 挂载点）；OpenCode Web 的项目列表/最近会话存在**浏览器 IndexedDB**（iframe 与新窗口各自独立，服务端无法预置），首次使用需「添加项目 → 输入 `/` → 点击 `~`」恢复，会话回放走 runtime client 的 `session_history(after)` 与 SSE `after_seq` 游标。
- **OpenCode Web 研究工作台前端整合边界（#111，已合并 AIResearch；#121 重构后 iframe 直连 + #122 写操作自主执行 + #157 明文 URL）**：前端 `ResearchWorkbench` 页面（`/research/workbench`）顶部 Tab 分为「工作台」（**iframe 直连 OpenCode Web**：网关启用即签发明文 `web_url`（#157 无凭证，iframe 与「新窗口打开」共用同一 URL），无 conversation 绑定；顶部 gateway 状态 banner 展示运行/健康/容器 ID/版本/**FinBoard MCP 内嵌运行状态**）与「审批中心」（AI 草案 / 因子假设 / 审计日志的审计与历史查看入口，走 REST `/api/research/ai/*`，不依赖 OpenCode Web 网关）。**OpenCode Web 是研究交互层，OpenCode 自身管理会话/历史/恢复**（#121 移除了 FinBoard 侧 conversation 投影层，前端不再有会话列表/关键事件摘要/中断/中止）；**研究写操作（创建 ResearchRun/回测/模拟盘/因子/策略）由 agent 通过 MCP 自主执行**（#122 放开审批门），审批中心 Tab 从「写操作硬门」变为「审计/历史查看入口」，`DraftStatus` 生命周期仍可用于 AI 草案追溯但不再 block 写操作。`opencode_web_enabled=false`（网关 503）时「工作台」Tab 显示降级提示，「审批中心」Tab 仍可用。前端不连接实盘 broker/账户/订单/持仓/Kill Switch；后端 `/api/research/ai/*` REST 端点保留作为审批数据层。
- **MCP 研究能力全覆盖同步规范（#123）**：FinBoard 新增/修改研究功能时，**必须同步**（1）在 `finboard-mcp` 注册/更新对应 MCP 工具；（2）更新 `packages/finboard-mcp/src/finboard_mcp/server.py` 的 `_INSTRUCTIONS` 工具清单；（3）更新 Skill `.agents/skills/finboard-opencode-research/references/tools.md` 工具契约与 `SKILL.md` 工具选择表；（4）更新 `packages/finboard-mcp/ROADMAP.md`（如涉及新阶段）。否则 OpenCode Agent 的认知会与研究现实脱节。工具扩展路线图（#124-#128）见 ROADMAP.md。MCP 是受控入口，关闭即回滚，不影响 ResearchAssistant/REST 入口。
- **任务队列 MCP 工具边界（#136，统一持久化后台任务队列 #117 的 Sub-issue 4）**：`finboard-mcp` 把 #142/#143/#144 建立的持久化 `background_jobs` 队列以 4 个受控 MCP 工具暴露给外置 Agent：`finboard_job_list` / `finboard_job_get`（只读）、`finboard_job_enqueue` / `finboard_job_cancel`（写，受 `mcp_readonly_only` 门控）。**任务队列只服务研究 / 数据 / 回测类任务**（`background_jobs` 表，`BJ-` ID，与实盘 orders/fills/positions 完全隔离，不建任何外键）。`finboard_job_enqueue` 的 kind 白名单全是研究 / 数据域（`echo` / `research_run` / `feature_snapshot` / `bulk_download` / `dataset_publish` / `backtest_run` / `data_sync` / `fetch_all` / `quality_repair`），不含实盘能力；实盘交易内核（盘前检查 / 收盘撤单 / 日终核对 / Broker 心跳 / Kill Switch）由专用 `finboard-scheduler` asyncio 调度执行，**不进入**统一队列、不暴露为 MCP 工具。`job_cancel` 属研究域协作式取消（executor checkpoint 时退出），非实盘撤单，因此 `finboard_job_*` 与 `finboard_run_*` 同理豁免实盘交易动词检查。同 issue 顺带把 `feature_snapshot_job_start/status` 从已下线的进程内 `FeatureSnapshotJobManager` 迁移到持久化队列（与 #144 的 REST 口径一致）。回滚方案：移除 4 个 `finboard_job_*` 工具即可，不影响 #142/#143/#144 的 worker / 任务表 / REST 接口。
- **OpenCode agent 模型解钉与 provider 模型同步（#242，auth provider 扩展 #248）**：`finboard-researcher` 不再钉死模型——移除 `.opencode/agent/finboard-researcher.md` frontmatter 的 `model` 与 `.opencode/opencode.json` 中被 `.md` 覆盖的死配置 `agent` 块（**agent 唯一权威 = `.opencode/agent/*.md`**，默认模型跟随 OpenCode Web UI / 会话选择）。容器启动渲染 runtime `opencode.json` 时，对声明 `options.baseURL` 的 config provider（deepseek / zhipuai-coding-plan / opencode-go 均已声明）经 OpenAI 兼容 `GET /models` 拉取模型 id **只增不改**合并（key 解析顺序 = `{env:VAR}` 占位符 → auth.json 预读；都解析不到则跳过该 provider；结果缓存 `.opencode/runtime/provider-model-cache.json`，拉取失败回退缓存）。接入面（baseURL/headers/鉴权）永以仓库文件为准，远端响应只贡献 `models` id，凭证**永不**写入渲染产物；渲染在宿主机侧完成、容器 `:ro` 挂载，agent 不可写。settings `opencode_model_sync_enabled`（默认开；false 即回滚，渲染期零网络请求）。配置存储事实：auth 连接的 provider 持久化在 data 卷 `auth.json`（UI 徽标「API 密钥」），config 声明的 provider 徽标「配置」，同 id 与 models.dev 目录合并；**opencode 内置目录是构建时打进镜像的快照，运行时不刷新**（#248 实测：镜像 2026-08-07 构建，活体目录已有 glm-5.3 而容器缺新模型）——auth 连接 provider 的新模型经本同步机制获得：仓库声明其 baseURL（不写 key），管理器启动时用一次性容器 `:ro` `cat` data 卷 auth.json 预读凭证注入同步，docker 不可用/卷缺失则静默跳过。纯研究运行时配置，不连 broker 不下单。

## Git 工作流

### 分支模型
- `main` — 稳定发布
- `dev` — 集成分支，所有新特性从这里切出
- `m/<里程碑>` — 里程碑分支
- `feat/<issue-slug-id>` — 功能分支，从 `dev` 切出

### 开发流程（严格按序）
0. 在github仓库创建issue和对应里程碑（可选）
1. 从最新的 `dev` 切出 `feat/<issue-slug-id>`
2. 本地完成开发，**本地须通过 CI**
3. 用 `gh pr create` 创建 PR（目标分支为对应里程碑或 `dev`）
4. **等待用户确认合并** — 这是整个流程中唯一需要用户确认的步骤
5. PR 合并后会自动关闭对应 issue，不要手动关

feat 分支只能通过 PR 合并进里程碑或 `dev`，禁止直接 push 合并。

### 多 worktree 并行开发（多 agent，#289）

主目录是第一个 agent 的常驻工作区；更多 agent 各用独立 git worktree，禁止共用
工作目录、禁止在他人工作区 checkout / merge 对方分支、禁止 checkout 别的 worktree
已持有的分支（git 本身会拒绝）。昂贵数据（`data_cache` ≈680MB / `data_releases`
≈2.0GB）只存主目录一份，worktree 以 NTFS junction 共享，重新拉取不可接受。

**一键创建（推荐，幂等可重跑）**：

```bash
scripts/create-worktree.sh <N> <feat分支名> [base=origin/m/<里程碑>]
# 例: scripts/create-worktree.sh 3 feat/my-issue-290 origin/m/research-backtest
```

脚本依次完成：worktree add（主目录同级 `FinDashboard-wtN`）→ junction 共享
`data_cache` / `data_releases` → 复制并改写 `.env`（独立库 + 端口 + 关闭
OpenCode/MCP）→ 建库 `findashboard_wtN` / `findashboard_wtN_test` →
`uv sync`（复用主目录 `.uv-cache`）→ `alembic upgrade head` → `web npm install`。

**手工等价步骤与硬性规则**：

- **数据共享**：`cmd //c "mklink /J data_cache <主目录绝对路径>\data_cache"`
  （`data_releases` 同理）。**禁止 `ln -s`**（Git Bash 默认是复制而非链接）；
  **禁止对 junction 执行 `rm -rf`**（Git Bash 会穿透链接删掉主目录真实数据），
  删除链接只能 `cmd //c rmdir <名字>`。
- **隔离配置**：pydantic 按 CWD 读 `.env`，每 worktree 一份——`FINBOARD_DB_URL` /
  `FINBOARD_TEST_DB_URL` 指向 `findashboard_wtN` / `findashboard_wtN_test`；
  `FINBOARD_OPENCODE_ENABLED` / `FINBOARD_OPENCODE_WEB_ENABLED` /
  `FINBOARD_MCP_ENABLED` 置 false——OpenCode / MCP / 研究运行时全局锚定主目录
  （容器名与端口全局唯一，第二个实例会撞名）。
- **端口分配（wtN，N≥2）**：API `8000+N`、前端 `5172+N`、MCP（如启用）`8763+N`；
  启动用 `make dev API_PORT=8002 WEB_PORT=5174`（vite 端口 / API 代理目标支持
  `FINBOARD_WEB_PORT` / `FINBOARD_WEB_API_PORT` 覆盖；显式指定端口被占用即报错，
  不静默换端口——静默换端口后代理会指到别的 worktree 的后端，预览到错误数据）。
- **依赖**：`UV_CACHE_DIR=<主目录>/.uv-cache uv sync --all-packages` 一次性复用
  主目录下载缓存；`web/node_modules` 各 worktree 独立 `npm install`，不共享
  （两端 install 会互相踩）。测试库无需 alembic（conftest `ensure_test_db` +
  `create_all` 自动建表），开发库须 `uv run alembic upgrade head`。
- **纪律**：进 worktree 先切 feat 分支再动代码，不直接在 `m/*` 分支提交；pytest
  仍在各 worktree 根目录运行（`--basetemp .pytest-tmp` 相对各自根，见下文
  Windows ACL 约定）；PR / issue 流程与单工作区完全一致。
- **回收**：先 `cmd //c rmdir` 两个 junction，再 `git worktree remove
  ../FinDashboard-wtN`；数据主体在主目录，删除 worktree 不影响缓存与发布数据。

### 提交信息
- 格式：`<分类>: <修改点描述>`，分类如 `feat` / `fix` / `refactor` / `docs` / `chore` 等
- 描述要点到修改层面即可，不要罗列具体代码行
- **禁止添加任何 `Co-Authored-By` 署名**（包括 Claude / Cursor / ChatGPT 等所有 AI 助手）

### Issue 规范

所有进入开发的事项必须以 GitHub issue 形式登记，字段缺失的 issue 不予开工：

- **背景**：为什么做这件事（业务 / 技术 / 风险驱动力）。一句话能说清的不要写两段。
- **问题 / 需求描述**：具体要解决什么。现状（现有代码、日志、数据）是什么，期望状态是什么。触及交易安全红线的必须明确标出。
- **相关 issue / PR**：列出前置、后继、重复、相关项，用 GitHub 关键字关联：`blocked by #N` / `blocks #N` / `related to #N` / `duplicate of #N`。
- **验收标准（Acceptance Criteria）**：以可勾选清单形式给出 pass/fail 条件，至少覆盖：
  - 功能点（映射到 `phase1_doc.md` §3.4 端到端验收步骤中相关的条目）
  - 测试要求（单元 / 集成 / 故障注入）
  - 文档变更（AGENTS.md / README / 接口契约）
  - 风险要求（是否触及交易安全红线、是否需要用户确认）

### PR 规范

PR 是变更进入 `dev` / 里程碑分支的唯一入口。PR 描述必须包含以下字段：

- **实现情况**：逐条对照 issue 验收标准说明完成情况；未完成项必须显式标出并说明原因，不能偷偷漏掉。
- **关键决策**：实现过程中做出的技术选型、取舍与理由。为什么 A 方案而不是 B 方案、跳过某项检查的原因、新引入的依赖、对外接口的变更。
- **Know-how / 坑位**：沉淀开发过程中的经验 —— 调试技巧、踩到的坑及根因、规避方式、参考链接。目标是让后续接手的人能快速复用、不重蹈覆辙。
- **测试与验证**：本地执行了哪些命令（lint / typecheck / test 的具体命令与用例），结果如何；CI 状态。
- **风险与回滚**：是否触及交易安全红线（`client_order_id` 唯一性 / 下单超时不重试 / 持仓真实来源 / 重启恢复 / Kill Switch）？触及的话是否已与用户确认？回滚方案（迁移 down / feature flag / 配置回退）是什么？

PR 标题遵循提交信息规范（`<分类>: <修改点描述>`），并在描述中用 `Closes #N` 让合并后自动关闭对应 issue。

## 常用工具
- `gh` — 创建 PR、issue、查看 CI 等 GitHub 操作的首选方式

## 记忆（Memory）规范

- **记忆文件统一存放在仓库 `docs/memory/` 下**，一篇记忆一个 Markdown 文件
  （`<kebab-case-slug>.md`），内容与格式不限但至少包含：主题、结论 / 事实、
  **Why（为什么）** 与 **How to apply（下次如何应用）**；相对日期一律转绝对日期。
- **`docs/memory/README.md` 是唯一索引**：每个记忆文件一行指针
  （`- [标题](文件.md) — 一句话钩子`），新增 / 修改 / 归档记忆时同步更新。
- **禁止把记忆写入任何 coding 框架自有的路径**（如各框架的 `~/.` 记忆目录、
  会话目录、私有缓存），保证记忆可在不同框架 / 工具间共享协作。
- 仓库中已能直接查到的事实（代码结构、git 历史、AGENTS.md 本身、issue/PR 描述）
  不重复记录；只记「不读代码 / 不翻历史就不知道」的经验与决策（踩坑根因、
  环境特殊性、用户偏好、跨 issue 的上下文）。
- 记忆属于项目资产，随 PR 进仓库（与代码 / 文档一样走评审与合并），不留在
  本地私有位置。

## 当前仓库状态

P0-P4 已完成（issue #1-#14 全部关闭）。P5 大部分完成（策略框架 + 行情事件已实现，策略状态持久化待验证）。当前进入**实盘环境验证**阶段：QMT 真机调试 + 标的元数据 + 连续运行验证 → 第一个真实策略 → 小资金实盘。

### 构建 / 测试 / lint / typecheck 命令

```bash
# 运行全部测试（需 PostgreSQL 运行在 127.0.0.1:5432）
uv run pytest tests/ -v

# 仅单元测试（不需 DB）
uv run pytest tests/unit/ -v

# 仅集成测试（需 DB）
uv run pytest tests/integration/ -v

# Lint
uv run ruff check packages/ tests/

# Type check
uv run mypy .

# DB 迁移
uv run alembic upgrade head
```

**pytest 临时目录（Windows ACL 根除，勿绕行）**：所有 pytest 调用统一使用仓库内
`.pytest-tmp/`（`pyproject.toml` addopts 已固定 `--basetemp .pytest-tmp` +
`-p no:cacheprovider`，该目录已入 `.gitignore`）。原因：`%LOCALAPPDATA%` 下默认
`pytest-of-<user>` 与 `.pytest_cache` 在本机偶发 WinError 5 拒绝访问（ACL 损坏），
导致 `tmp_path` 系 fixture 报错。因此**所有 pytest 必须在仓库根目录运行**（相对路径
以 CWD 为基准），不要手动加 `--basetemp` 指向系统临时目录，也不要为绕过报错而
关闭/改写该配置。若仓库根目录没有 `.pytest-tmp`，pytest 会自动创建；CI 与本地行为一致。

### Monorepo 结构

```
packages/
  finboard-shared/     — 领域模型 / 类型 / 异常 / ID 生成
  finboard-persistence/ — SQLAlchemy ORM / Repository / Alembic 迁移
  finboard-broker/      — BrokerAdapter / MarketDataAdapter 抽象 + Mock 实现 + factory
  finboard-broker-qmt/  — QMT (xtquant) 适配器 — 交易 + 行情(xtdata)（Windows-only）
  finboard-core/        — TradingKernel / OrderManager / PositionManager / 状态机 / EventBus
  finboard-risk/        — PreTradeChecker / KillSwitch / RiskConfig
  finboard-reconcile/   — ReconciliationEngine（只读核对）+ RecoveryEngine（状态修复）
  finboard-scheduler/   — asyncio 定时任务调度（盘前检查 / 收盘撤单 / 日终核对 / 连接心跳）
  finboard-app/         — 组装根 / CLI / 配置
  finboard-api/         — FastAPI REST + WebSocket API（人工交易控制台后端）
  finboard-mcp/         — FinBoard MCP Server（向 OpenCode 等外置 Agent 暴露受控研究工具，issue #108）
  finboard-opencode/    — OpenCode 研究运行时集成（会话关联/SSE 事件/中断恢复，issue #109）
```
